#
# https://help.github.com/articles/dealing-with-line-endings/
#
# These are explicitly windows files and should use crlf
*.bat           text eol=crlf

# Integration acceptance fingerprints require the exact committed fixture bytes.
src/integration-test/resources/** -text

# Terrain golden dumps and diagnostics are canonical cross-platform contracts.
render-contracts/src/test/resources/** -text
