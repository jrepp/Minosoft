<!-- Copyright (C) 2026 Jacob Repp -->

# Graphics evidence map

## Boundary

Graphics owns window/context integration, OpenGL resources, renderer lifecycle,
cameras, chunk/entity meshes, shaders/textures, lightmaps, sky/fog, particles,
in-game GUI/HUD, and audio presentation.

## Evidence map

| Status | Claim | Evidence |
| --- | --- | --- |
| Observed | A `Rendering` instance is composed for a `PlaySession`. | `gui/rendering/Rendering.kt` and the current session establishment path. |
| Verified | OpenGL 3.3 remains the compatibility baseline, while non-Apple GLFW launches request a 4.3 core context first and may receive a newer compatible context. macOS requests 4.1 before 3.3 because Apple supports OpenGL only through 4.1. Compute programs and shader-storage buffers remain capability-gated on actual 4.3 support rather than inferred from the request. | `OpenGlContextRequest`, `GlfwWindow`, `OpenGlCapabilityDiagnostics`, focused negotiation tests, Apple's recorded 4.1 runtime evidence, and [2026-07-30 architecture trajectory evidence](../evidence/2026-07-30-architecture-trajectory.md). |
| Verified | GUI-independent graph/resource declarations, generation lifecycle, fixed-storage terrain telemetry, normalized identity/coverage/interoperability, checked coordinate/depth/material metadata, bounded urgency-aware CPU scheduling and completion mailbox, atomic whole-pipeline generations, runtime scopes, residency/failure/submission contracts, transactional view publication, distant source-authority merges, and versioned bounded diagnostic schemas compile and test in the dependency-clean `render-contracts` module. Concrete world, window, LWJGL, and OpenGL implementations depend on that module and may not be introduced below the boundary. | `render-contracts/`, `TerrainBuildRuntime`, `TerrainSelectionPublication`, root project dependency direction, `:render-contracts:test`, and the [terrain consolidation implementation start](../evidence/2026-07-30-terrain-consolidation-start.md). |
| Observed | Renderer and GPU resources expose explicit unload lifecycles. | Renderer, mesh, buffer, framebuffer, texture, light, and sound implementations. |
| Observed | Shader objects already expose a reload operation. | `gui/rendering/shader/Shader.kt` and `system/opengl/shader/OpenGlNativeShader.kt`. |
| Verified | The exact Bliss 2.1.0 archive publishes through dedicated `dh_terrain`, `dh_water`, and `dh_shadow` routes. Built-in retains the host lens with an independent LOD far plane and separate solid/water families. The graph owns distant solid/water phases, an independent pre-water depth snapshot, compact material/normal decoding, and typed camera/projection state. Native, persisted, local, and managed-server pages converge before one balanced page hierarchy builds semantic top/bottom/side geometry with deterministic incomplete-neighbour skirts. Region/material/view batching, frame-pinned near masking, and ordered distant-solid/depth/water submission replace the retired detached whole-domain tile planner. Built-in and Bliss exact seam scenarios pass on Apple OpenGL 4.1; second-driver qualification remains release-matrix work. | `DistantTerrainRenderer`, `DistantHierarchicalTerrainRuntime`, `DistantTerrainRegionArtifactEncoder`, `DistantHorizonsLodController`, `IrisShaderPackPlanner`, `IrisLegacyShaderTransformer`, focused tests, `render.substrate`, [terrain seam pixels](../evidence/2026-07-31-terrain-seam-pixels.json), and the [terrain consolidation evidence](../evidence/2026-07-30-terrain-consolidation-start.md). |
| Verified | Built-in DH water renders opaquely at LOD scale and discards marked water-bed depth-support quads, preventing the coarse seabed from appearing as a pale ring at the native handoff. The marker is above the eight-bit DH material field, so authored Iris/Bliss routes retain their masked material ID and required pre-water depth input. Dynamic player skins use identity bake-time UVs and remain draw-time texture bindings, which restores the ordinary first-person hand. | `DistantTerrainRegionArtifactEncoder`, built-in distant terrain shaders, `PlayerSkinUvTexture`, focused tests, generation-7 same-pose captures, and the [DH/Bliss integration evidence](../evidence/2026-07-29-distant-horizons-bliss-integration.md). |
| Verified | Detached distant columns, vertical pages, bounded storage, source provenance, renderer-facing diagnostics, render source/configuration seams, renderer, shader, hierarchy, and page mesher are owned by neutral `terrain.distant` and `gui.rendering.terrain.distant` packages. Fabric imports those contracts inward for exact-artifact activation, ingestion, read-only v1 migration, schema-v2 persistence/protocol, settings, debug translation, and renderer registration. Near and distant batching consume one dependency-clean transactional view-publication contract, and the complete atomic diagnostic exposes bounded provider-neutral visibility/publication and residency/submission state. The production rollout switch, whole-domain distant planner/uploader, and near cause alias are retired; the architecture ratchet prevents their return. | `terrain.distant`, `DistantHierarchicalTerrainRuntime`, `DistantTerrainRenderer`, `TerrainSelectionPublication`, `TerrainProductionDiagnosticCapture`, `DistantHorizonsCompatibilityAdapterTest`, `TerrainArchitectureBoundaryTest`, and the [terrain consolidation implementation start](../evidence/2026-07-30-terrain-consolidation-start.md). |
| Verified | GLSL preprocessing tracks block-comment state instead of discarding every operator-led line. Multiline expressions beginning with `*`, inline comments, and quoted comment markers remain distinct, and the packed-light color shader reaches live render-ready compilation. | `GLSLShaderCode`, `GLSLCommentStripperTest`, and the `naturalist-terralith-2026-07-24` live Fabric-stack launch. |
| Verified | Non-GPU logic can use dummy rendering-system implementations. | `src/integration-test/kotlin/de/bixilon/minosoft/gui/rendering/system/dummy/`. |
| Verified | One production `RenderGraphGeneration` now orders sky, terrain material classes, entities, block entities, particles, overlays, HUD, and composite passes with stable IDs and explicit semantics. World producers declare required-ID `WorldRenderPass` records through `WorldPassRegistry`; the former `PipelineElement`/`LayerSettings` translation layer is retired. Base, Sodium, Iris-reference, and combined profiles change provider ownership without changing the frame architecture. | `RendererPipeline`, `RenderGraph`, `WorldRenderPass`, `WorldPassRegistry`, focused pipeline/graph tests, `render.substrate`, and [R0–R7 checkpoint](../evidence/2026-07-24-render-substrate-r0-r7.md). |
| Verified | Terrain and shader registries share one `TransactionalOverrideStore` for fallback publication, stale-registration safety, frame leases, and deferred retirement. The retained fallback is never retired while it may be republished and closes exactly once after every fallback lease drains. Renderer-specific candidate validation and GPU cleanup remain at their owner boundary. | `TransactionalOverrideStore`, `TerrainBackendRegistry`, `ShaderPipelineRegistry`, headless store tests, and focused registry tests. |
| Observed | Model code still holds some renderer state. | `data/entities/entities/Entity.kt` and related entity/render coupling. |
| Verified | The pinned Sodium compatibility adapter negotiates the preserved exact descriptor and installs settings, debug, and renderer-event translation. Neutral `TerrainProviderSelection`, `TerrainProviderRendererBridge`, and `ChunkRendererNearTerrainBackend` own selection, lifecycle, and concrete delegation outside Fabric; Fabric retains only the exact-artifact builder and callbacks. `TerrainBackendRegistry` pins the exclusive generation through a complete frame, rejects duplicate view/material submissions, and restores the built-in provider on toggle/unload. The backend still delegates Minosoft's chunk core, so full optimized behavioral ownership is not accepted. | `SodiumCompatibilityAdapter`, `SodiumRendererHookBuilder`, `TerrainProviderSelection`, `TerrainProviderRendererBridge`, `ChunkRendererNearTerrainBackend`, `TerrainBackendRegistry`, focused tests, the [terrain consolidation implementation start](../evidence/2026-07-30-terrain-consolidation-start.md), and [R0–R7 checkpoint](../evidence/2026-07-24-render-substrate-r0-r7.md). |
| Verified | Iris exact-artifact activation retains shader planning, presentation controls, reload events, and diagnostics in Fabric, while the Minosoft renderer lifecycle shell is the neutral `ShaderPipelineRendererBridge`. Together with the neutral Sodium bridge, this leaves the Fabric renderer/provider architecture ratchet with an empty allowlist. | `IrisCompatibilityAdapter`, `ShaderPipelineRendererBridge`, `TerrainArchitectureBoundaryTest`, focused Iris/adapter tests, and the [terrain consolidation implementation start](../evidence/2026-07-30-terrain-consolidation-start.md). |
| Verified | The shared terrain core now emits four independent packed light/color values, selects a brightness/light-aware quad diagonal, and applies corner-sampled block/sky light plus continuous AO to full, partial, and inset solid faces and every fluid face. A section-local tint cache supplies half-block-offset bilinear grass, foliage, and water colors over the configured radius sampler; every blending setting invalidates terrain. The modern Iris terrain transform decodes color and packed light from the same canonical attribute. A live Sodium-owned canary showed smooth swamp/plains grass and water transitions plus torch/stair/farmland lighting. | `SmoothTerrainLighting`, `TerrainTintCache`, solid/fluid meshers, `ChunkMeshBuilder`, `IrisLegacyShaderTransformer`, focused tests, and [Sodium visual-fidelity evidence](../evidence/2026-07-27-sodium-visual-fidelity.md). |
| Verified | Production solid/fluid geometry, AO, smooth light, tint, and fluid helpers consume detached snapshots, while accepted block-entity renderers are materialized on the render thread. Near candidates always carry a dependency-clean semantic artifact and deterministic digest, and near+distant builds share one process CPU service. The supported-version headless matrix freezes water, lava, solid, light, tint, and entity semantics. OpenGL consumes artifacts through bounded transactional region storage, cached region/material/view batches, GL fences, and multi-draw; the conventional per-section draw loop remains the non-region fallback. Fixed recovery diagnostics distinguish CPU-leased, pending-submission, failed-submission, and device-invalidated retired pages and retain failed ranges until explicit recovery. | `TerrainBuildSnapshot`, solid/fluid meshers, `TerrainMeshArtifact`, `NearTerrainSemanticParityTest`, `TerrainProcessBuildService`, `TerrainSubmissionSequencer`, `terrain.runtime.storage`, `OpenGlNearTerrainRegionRuntime`, focused tests, and the [terrain consolidation implementation start](../evidence/2026-07-30-terrain-consolidation-start.md). |
| Verified | Dependency-clean distant contracts retain bounded vertical runs, one normalized voxel sampler, block/skylight propagation, a bounded schema-v2 codec, deterministic parent reduction, independent revisions, one page-budgeted best-first balanced selection, and semantic page meshes. Native, local, managed-server, network, and persisted sources publish one page model; readable v1 tiles migrate one-way. The production hierarchy builds and transactionally replaces page-local artifacts through the shared CPU service, retains page-relative geometry across origin movement, publishes independent main/shadow selections, and masks both views from one frame-pinned near-coverage snapshot. Protocol/reconnect, server TPS, checked seam pixels, movement scaling, and Apple resource gates pass. | `terrain.distant.hierarchy`, `terrain.distant.lighting`, `TerrainCoveragePageMask`, `DistantWorldVerticalSampler`, `DistantTerrainProtocolV2`, `DistantHierarchicalTerrainRuntime`, focused tests, retained live records, and the [terrain consolidation implementation start](../evidence/2026-07-30-terrain-consolidation-start.md). |
| Verified | The distant renderer owns a compact provider descriptor distinct from the near layout, with opaque/water, main/shadow, block/sky-light, resolved-tint, buffer-update, and foreign-profile declarations. Upload failure retains last-known-good geometry; shader replacement cancels stale work and rebuilds retained pages. Graph tests pin opaque/depth/water order and Iris depth/shadow routes. Exact Complementary and Bliss routes compile and submit on Apple OpenGL, survive reload, restore resource baselines, and pass the built-in/Bliss checked-pixel seam scenarios. | `DistantTerrainInterop`, `TerrainPipelineCandidate`, `DistantTerrainRenderer`, `DistantHierarchicalTerrainRuntime`, `TerrainUploadBudgetScheduler`, `IrisLegacyShaderTransformer`, graph/shader/interop tests, exact-pack/live checks, and the [terrain consolidation implementation start](../evidence/2026-07-30-terrain-consolidation-start.md). |
| Verified | Near terrain meshing consumes frozen identity and the dependency-clean bounded scheduler/mailbox. Near and distant share one process CPU service and one render-context device identity/serial source. `ProductionTerrainPipelineRegistry` directly pins the existing backend and shader generation leases for a frame; it no longer nests a second generation registry or material/provider wrapper set. Both domains publish transactional selections; diagnostics align world, provider/layout, material/shader/graph, coverage, scheduling, residency, recovery, submission, and bounded page state. Typed failure backoff/quarantine and epoch barriers reject stale work. The rollout switch, whole-domain distant worker/uploader, and near cause alias are retired. | `TerrainBuildRuntime`, `TerrainSharedBuildService`, `TerrainProcessBuildService`, `TerrainSubmissionSequencer`, `TerrainSelectionPublication`, `ProductionTerrainPipelineRegistry`, `TerrainProductionDiagnosticCapture`, `TerrainRegionStorage`, `World.terrainEpoch`, `ChunkMeshingQueue`, `DistantTerrainRenderer`, focused tests, live records, and the [terrain consolidation implementation start](../evidence/2026-07-30-terrain-consolidation-start.md). |
| Verified | The production terrain path records queue wait, snapshot, mesh, worker-busy, visibility, and real buffer-upload latency in fixed primitive histograms. Snapshot sample counts derive from the captured buckets, preventing concurrently polled percentiles from observing a newer count than their buckets while removing one hot-path atomic increment. Exact saturating counters/gauges expose queue and upload depth, current/high-water active workers, cumulative utilization, cancellation/staleness/rejection/failure outcomes, visible sections, and output/upload bytes. Instrumentation can be disabled without clock reads or state mutation; an alternating identical-workload test rejects a statistically meaningful p95 regression. | `TerrainPerformanceTelemetry`, `ChunkMeshingQueue`, `ChunkVisibilityManager`, `MeshLoadingQueue`, `ClientDebugChannel`, `TerrainPerformanceTelemetryTest`, and [terrain runtime architecture](../../design/terrain-runtime-architecture.md). |
| Verified | Loaded terrain and the renderable visibility list publish as one revision-checked transaction. A traversal snapshot whose loaded-mesh revision changed cannot replace the current visible list, while upload/replacement removes the old visible mesh and inserts the new one in sorted position under the loaded-map write lock before the old GPU buffer can be queued for unload. Every replacement still invalidates traversal because direct insertion alone cannot prove that a section is not occluded. The bounded traversal uses primitive grid storage for implicit passable air instead of materializing a dense section-node map. | `LoadedMeshes`, `ChunkVisibilityManager`, `VisibleMeshes`, `TerrainVisibilityTraversal`, `ChunkRendererTest`, live `render.substrate`, and [motion-noise diagnosis](../evidence/2026-07-28-cloud-foliage-motion-noise-diagnosis.md#terrain-publication-crash-and-motion-flicker). |
| Verified | Fabric compatibility adapters can independently intercept the native entity-visibility decision and frame queue-flush boundary through owner-keyed hooks; registrations disappear when the pack scope closes. | `FabricEntityVisibilityHooks`, `FabricFrameHooks`, `EntitiesRenderer`, `RenderLoop`, focused tests, and [Fabric stack evidence](../evidence/2026-07-21-fabric-stack.md). |
| Verified | The exact Iris adapter selects a transactional shader-pipeline generation pinned for the complete frame. Its typed logical `colortex`, `depthtex`, `shadowcolor`, and `shadowtex` plan owns real OpenGL textures/FBOs, routes declared outputs and samplers, snapshots requested depths, generates requested mipmaps at the consuming program, clears integer and normalized targets through matching APIs, and ping-pongs color buffers. Explicit graph boundaries execute numeric begin, shadow-composite, prepare, deferred, and composite families in Iris order before final presentation; a missing pack final receives a bounded identity presenter. A project fixture also executes material-specific terrain/shadow fallbacks, 56 negotiated main-view scene variants, six shadow scene ABI variants, immutable frame state, and explicit physical casters. Every current world geometry producer enters through a semantic layer and declares an immutable family/vertex/state contract; with an override selected, an unclassified scene shader now fails at bind time instead of silently activating its host program. Internal composites and the no-plan built-in/headless path remain explicit exceptions. Per-draw program specialization preserves the host vertex/state ABI while routing line/hitbox, leash, emissive-eye, armor-glint, beacon-beam, lightning, and explicit non-opaque first-person Gecko item layers through the exact pinned Iris fallback families. Program discovery is dimension-scoped: base, exact/wildcard `dimension.properties`, and legacy `world0/world-1/world1` sets cannot mix duplicate roots, while shared includes remain pack-wide. Bounded option-driven `program.<path>.enabled` conditions are applied before stage pairing, current/legacy particle-order properties select explicit before/after-deferred graph phases, and `separateEntityDraws` splits blended entity and Gecko block-entity layers into distinct post-deferred programs without replaying their opaque bases. Block-entity collection phases do not prebind a terrain wrapper; every nested renderer owns its exact contract. `skipAllRendering` suppresses only main terrain/entity/block-entity producers while preserving shadows, post-processing, and the remaining scene. A dedicated transparency barrier snapshots `depthtex1` after opaque scene producers and before every translucent producer. Standard whole-producer shadow directives control the auxiliary view and terrain/entity/block-entity caster set; unrepresentable player-only, light-emitter-only, and translucent-shadow subsets fail closed. Live generation-42 diagnostics proved all five fullscreen families advanced once per frame through a 30-pass graph, the deferred-sensitive `after` particle default, enabled separate entity routing, explicit shadow routing, five logical/seven physical shader textures, zero fallback binds, and stable linked-resource state. A generation-46 live property toggle additionally proved path-tracing geometry suppression leaves fullscreen, shadow, sky, cloud, and hand programs advancing, then restores the original pack generation without leaks. Generation 50 compiled 39 main-view variants and live-selected `gbuffers_line` from the block-outline producer. The subsequent producer generation compiled all 56 variants, including `gbuffers_beaconbeam`, `gbuffers_lightning`, and `gbuffers_hand_water`, with 20 live textures, seven framebuffers, 105 linked programs, zero shader objects, zero fallback binds, and stable target/program ownership. A longer soak found and removed two false fallback keys caused by the former block-entity wrapper. Source hot reload now also leaves Iris GPU retirement to render-thread context unload instead of deleting programs from the JVM shutdown hook. A later fail-closed hot reload survived more than 1,100 producer-rich frames with sun/moon, line, entity, terrain, shadow, cloud, sky, and hand selections, no missing-contract failure, and the same empty fallback/resource baseline. The terrain backend submission ledger and independent `selectedTerrainBinds` counters agree on main opaque/cutout/translucent and shadow opaque/cutout paths, including the exact solid/generic/shadow program roots. Unsupported stages and unbound inputs still reject before publication. Independent shader-pack acceptance, producer-rich live translucent entity/block-entity, emissive/glint, beacon/lightning/hand-water scenes, the remaining uniform/property catalog, custom images, compute-capable backends, a real multi-dimension transition, and broader producer canaries remain open. | `IrisCompatibilityAdapter`, `IrisShaderPackPlanner`, `IrisWorldShaderPipeline`, `IrisOpenGlRenderTargets`, `IrisFrameState`, `SceneShaderContract`, `ShaderPipelineRegistry`, `RendererPipeline`, `ChunkRenderer`, `BeaconBeamRenderer`, `LightningBoltRenderer`, `HeldItemRenderer`, `EntityDrawer`, `GeckoLibBlockEntityRenderer`, `render.substrate`, focused tests, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Pinned-frame ownership now closes the last generic host-shader escape hatch. When an Iris plan owns the frame, any `SCENE_GEOMETRY` shader bind must occur inside an explicit semantic pass or renderer-owned internal-target scope; an unscoped source-level callback fails instead of drawing a host-shaded copy that is absent from Iris route diagnostics. Resource loading outside the frame, HUD/presentation composites, and the built-in/headless pipeline remain legal. The focused boundary predicate and exact high-feature Complementary gate pass. A producer-rich hot-reloaded client advanced through frame 731 with empty rejected/fallback ledgers and a stable 40/7/107/0 texture/framebuffer/program/shader ledger. | `ShaderPipelineRegistry.requireFrameSceneSemantic`, `ShaderPipelineRegistryTest`, `RendererPipeline`, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Specialized Iris selection is now lexical across the complete draw. Uniform setters and skeletal mesh helpers may re-enter `Shader.use`, but cannot replace `ENTITY_EYES`, `ARMOR_GLINT`, `HAND_WATER`, `LINE`, or an item entity specialization with the host shader's default family before submission. Every specialized producer was migrated off the former one-shot bind. Ordinary dropped/item-display block-model meshes retain their physical `BLOCK_FEATURE/BLOCK` ABI while selecting the entity family, matching the pinned Iris `ShaderKey` catalog; dormant `ProgramId.Item` has no selectable Iris 1.7.2 key and is not treated as the item-entity route. Real OpenGL vertex submission now records bounded per-contract draw and vertex ledgers, so acceptance distinguishes an intermediate bind from the program still active at `glDraw*`. A reversible live diamond item canary submitted only `ENTITIES_TRANSLUCENT/ENTITY/gbuffers_entities/BLOCK_FEATURE/BLOCK` plus its shadow specialization—568 draws and 3,408 vertices each—with no parallel block-family draw, then stopped all four counters on removal. | `Shader.withProgramFamily`, `ShaderPipelineRegistry.recordDraw`, `IrisWorldShaderPipeline.recordSceneDraw`, `OpenGlVertexBuffer`, `ItemFeature`, `HeldItemRenderer`, `SkeletalFeature`, `PlayerModel`, `VanillaArmorFeature`, `HitboxFeature`, `IrisLegacyShaderTransformer`, `IrisProgramFallbacks`, `render.prepare-item-entity`, pinned Iris `ShaderKey` bytecode, focused/exact-pack tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Complementary's modern `gbuffers_armor_glint` bridge now consumes Minosoft's physical `PLAYER_SKELETAL/PLAYER` armor mesh directly, including bone transforms, part/slot masking, inflation, and the explicit glint texture layer. Driver-bound diagnostics first exposed the prior `host:PlayerShader` fallback, then proved the corrected route submitted only `ENTITIES/ARMOR_GLINT/gbuffers_armor_glint/PLAYER_SKELETAL/PLAYER`. Armor removal also keeps the owner updateable for one final synchronization frame, clearing retained base/trim/glint entries instead of replaying four stale decoration draws indefinitely. A post-reload canary stopped at 2,568 glint draws/554,688 vertices across consecutive samples with empty rejection/fallback maps. | `IrisLegacyShaderTransformer.MODERN_GLINT_VERTEX_BODY`, `VanillaArmorFeature.retainsForSync`, focused/exact-pack tests, `render.prepare-vanilla-armor`, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Complementary now has driver-bound coverage for the complete compiled main-view scene ABI surface. `render.substrate` derives submitted vertex/state ABI sets from actual `glDraw*` calls and reports the compiled scene ABIs that have not yet submitted; bind-only selection is not accepted. Reversible production-path canaries cover held-water, lightning, weather, beacon, billboard text, flashing block, border, damaged block, translucent particle, first-person fire overlay, fixed sky texture, and sun scatter, in addition to the retained entity, item, armor, leash, flame, and terrain paths. One pinned generation submitted all 24 compiled scene-state ABIs and all 19 compiled scene-vertex ABIs with both missing sets empty, empty rejection/fallback ledgers, zero leases, and a stable 40-texture/7-framebuffer/112-program/0-shader ledger. Terrain submission remains intentionally independent and is checked through its backend ledger; fullscreen families remain generation-owned graph passes. | `WorldShaderPipelineDiagnostics`, `IrisWorldShaderPipeline.recordSceneDraw`, `ShaderPipelineRegistry.recordDraw`, `SkyboxRenderer`, `SunScatterRenderer`, `FireOverlay`, reversible `render.prepare-*` canaries, focused tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | A follow-up Iris producer audit supersedes the ordinary held-item split gap above. Ordinary item quads are bucketed by declared texture transparency: opaque/cutout geometry selects `hand`, while a separately blended translucent mesh selects `hand_water`. Pinned-jar inspection also proves `setup` is compute-only and that no Iris 1.7.2 `ShaderKey` selects `ProgramId.EntitiesGlowing`; Minosoft keeps outlines as an internal mask/composite and rejects dormant scene bridges instead of claiming executable support from enum membership. `selectedSceneContracts` records the requested semantic/family beside the selected pack fallback root. On the managed Complementary client, ice, honey, slime, glass, and tinted-glass ordinary-item canaries all produced `HAND/HAND_WATER/gbuffers_hand/HELD_ITEM/HELD_ITEM`, while their opaque/cutout faces produced the parallel `HAND/HAND` contract. This proves the live producer reaches `hand_water` even though the pack legitimately falls back to its `gbuffers_hand` source. The checked frame showed one held model, both rejection/fallback maps were empty, and the prior empty hand was restored. Other specialized producer canaries remain open. | `HeldItemMaterialConsumer`, `HeldItemRenderer`, `IrisProgramFallbacks.supportsSceneProgram`, `IrisWorldShaderPipeline.selectPrograms`, `WorldShaderPipelineDiagnostics.selectedSceneContracts`, `ClientDebugChannel.prepareTranslucentHeldItem`, focused and external-pack tests, pinned `iris-1.7.2+mc1.20.4.jar` bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The pinned Iris 1.7.2 fog, camera-precision, celestial, and world-phase inputs now cross the canonical pipeline. The immutable frame snapshot retains unsquared linear fog start/end, vec3/vec4 fog colors, linear mode, spherical shape, zero exponential density, and double-precision current/previous camera positions. It publishes Iris's exact floor-based `ivec3` integer plus vec3 fractional camera pairs, including correct negative-coordinate behavior, through a real OpenGL `glUniform3i` boundary. It also derives sun, moon, shadow-light, shadow-angle, and up vectors with the pinned Y/Z/X transform order. The planner resolves the GLSL `const float sunPathRotation` directive into the immutable generation, defaults it to zero, and rejects conflicting values. A separate per-bind state maps terrain materials and scene semantics/families to the explicit 24-value `WorldRenderingPhase` ABI; sun and moon remain distinct even though they share the planet mesh/state ABI. Unknown direct inputs still fail preflight, and diagnostics count actual standard uploads only when a linked program retains the uniform. The project fixture declares the camera split, normalized fog aliases, celestial family, a non-default 18-degree sun path, and representative terrain/shadow/entity/particle/sky/weather/hand stages. The hot-reloaded real-GL client selected fingerprint `54142e...a89` with `shaderSunPathRotation=18.0`, 56 main and six shadow variants, all five terrain/shadow routes, an empty fallback map, 20 textures, seven framebuffers, 105 programs, and zero live shader objects. Remaining property, image, feature, uniform/sampler, independent-pack, and producer-canary gates stay open. | `NativeShader`, `OpenGlNativeShader`, `IrisFrameState`, `IrisRenderStage`, `IrisShaderPackPlanner`, `ShaderPipelinePlan`, `IrisWorldShaderPipeline`, `SceneProgramFamily`, `PlanetRenderer`, `IrisFrameStateTest`, `IrisRenderStageTest`, `IrisShaderPackPlannerTest`, the `iris-reference` fixture, pinned `CameraUniforms`/`CelestialUniforms`/`FogUniforms`/`IrisInternalUniforms`/`PackDirectives`/`CommonUniforms`/`WorldRenderingPhase` bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The next pinned Iris frame-state slice adds previous model-view/projection matrices, local date/time/year vectors, camera-medium and movement/fire/ground flags, normalized survival health/hunger/armor/air, first-person/spectator state, HUD visibility, main-arm handedness, raw eye brightness in block/sky ×16 order, current sky color, and `pi`. Integer `ivec2` values cross a real `glUniform2i` boundary. The executable fixture retains every input, and focused tests prove the temporal, calendar, player, and exact `LightLevel(5,14) -> ivec2(80,224)` contracts. Full Java 17 unit/integration validation passed. Live fingerprint `a4913100...8d90` retained the 30-pass graph, 56+6 compiled scene variants, all fullscreen and terrain/shadow routes, producer-rich block/entity/particle/hand/line/sky selections, an empty fallback map, 20 textures, seven FBOs, 105 programs, zero shader objects, and no leases. Biome/ID-map, effect/mood, dynamic draw-state, custom image/uniform, Distant Horizons, compute, independent-pack, and remaining producer-canary gates stay open and fail closed. | `IrisFrameState`, `NativeShader`, `OpenGlNativeShader`, `IrisFrameStateTest`, `IrisShaderPackPlannerTest`, the `iris-reference` fixture, pinned `MatrixUniforms.Previous`/`IrisTimeUniforms`/`CommonUniforms`/`IrisExclusiveUniforms` bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The pinned Iris visual-effect family now uses shared vanilla 1.20.4 state instead of shader-only approximations. Status effects preserve the `-1` infinite sentinel, modern packets restore serialized Darkness factor state, and its 22-tick transition advances after duration ticking. The immutable frame snapshot publishes camera-entity `blindness`, `darknessFactor`, and `nightVision` plus the local-player lightmap `darknessLightFactor`; the native lightmap shares the corrected tick-based night-vision phase. Thirty-eight focused tests and full Java 17 unit/integration validation passed. Transactional live reload selected fingerprint `07e0228e...ba2c` while retaining the 30-pass/56+6-program graph, every fullscreen and terrain/shadow route, producer-rich traffic, an empty fallback map, 20 textures, seven FBOs, 105 programs, zero shader objects, and no leases. Mood, biome/ID-map, dynamic draw state, custom image/uniform, Distant Horizons, compute, independent-pack, and remaining canary gates stay open and fail closed. | `StatusEffectInstance`, `StatusEffectFactorCalculationData`, `EntityEffectS2CP`, `VisionEffectIntensity`, `NormalLightmapUpdater`, `IrisFrameState`, focused tests, the `iris-reference` fixture, pinned Iris `CommonUniforms`/`MixinLightTexture` and Minecraft `GameRenderer`/`LightmapTextureManager` bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The pinned Iris global smoothing family is now generation-owned rather than approximated or aliased. The planner resolves exact `wetnessHalflife`, `drynessHalflife`, and `eyeBrightnessHalflife` directives with 600/200/10 defaults and rejects invalid or conflicting generation values. `IrisFrameSmoothing` applies Iris's exponential half-life equation, selects wet versus dry decay by direction, smooths block/sky brightness independently, truncates to the exact `ivec2` ABI, treats zero as immediate, initializes the first sample directly, and resets transactionally with its `IrisWorldShaderPipeline`. Focused tests and full Java 17 unit/integration validation passed. Live fingerprint `d5869fc8...c841a` exposed exact fixture directives 40/12/4 while retaining the 30-pass/56+6-program graph, every fullscreen and terrain/shadow route, producer-rich traffic, an empty fallback map, 20 textures, seven FBOs, 105 programs, zero shader objects, and no leases. Biome/ID-map and dynamic draw-state are the next direct-uniform families; mood, custom image/uniform, Distant Horizons, compute, independent-pack, and remaining canary gates stay open and fail closed. | `IrisSmoothingDirectives`, `IrisFrameSmoothing`, `IrisFrameState`, `IrisShaderPackPlanner`, `IrisWorldShaderPipeline`, `ShaderPipelineRegistry`, focused tests, the `iris-reference` fixture, pinned Iris `PackDirectives`/`SmoothedFloat`/`CommonUniforms` bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The pinned Iris camera-biome family now samples the local player's block position through the immutable frame snapshot. `biome` follows Iris 1.7.2's fixed 65-key Minecraft 1.20.4 initialization order with zero for unknown/modded keys, not Minosoft's session registry index. `biome_category` follows the exact ordered legacy tag probe and 0–18 ordinal ABI; current and legacy host tag managers participate without changing that shader-facing contract. Position-aware precipitation maps none/rain/snow to 0/1/2, and rainfall/base temperature cross as floats. Forty-three focused tests and full Java 17 unit/integration validation passed. Live fingerprint `bdfe740e...1347` retained the five biome uniforms, 30-pass/56+6-program graph, every fullscreen and terrain/shadow route, producer-rich traffic, an empty fallback map, 20 textures, seven FBOs, 105 programs, zero shader objects, and no leases. Dynamic draw state is the next direct-uniform family; mood, custom image/uniform, Distant Horizons, compute, independent-pack, and remaining canary gates stay open and fail closed. | `IrisBiomeFrameState`, `IrisFrameStateClock`, `IrisFrameState`, focused tests, the `iris-reference` fixture, pinned Iris `BiomeUniforms`/`BiomeCategories`/`MixinBiomes` and mapped Minecraft `BiomeKeys`/`BiomeTags` bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Iris dynamic draw state now crosses the selected-program boundary after retained host synchronization. Continuation-aware `entity.properties`, `item.properties`, and ordered identifier/tag/property `block.properties` maps preserve Iris's absent/present missing defaults. Immutable nested scopes cover entity features and casters, name tags/projected shadows, opaque/translucent block entities, and first-person held items; terrain/fullscreen binds clear stale state. Linked declared uniforms receive entity/block-entity/current-item IDs, zero-default entity overlay, and an exact integer OpenGL blend-factor tuple. The frame snapshot supplies both held IDs, default-state block luminance, white light colors, and typed default-true `oldHandLight` behavior. Selected-program fog synchronization now writes through the transaction-supplied native target rather than reactivating the host shader; a strict OpenGL run found and removed the former wrong-program uniform-location error. Sixty-three focused tests and full Java 17 unit/integration validation passed. Live fingerprint `b2dd4a08...0ca8` retained the 30-pass/56+6 graph and empty fallback map while reporting 2,137,521 dynamic uploads, including `entityId=7` creature traffic and `blockEntityId=41` chest traffic, with no subsequent shader/uniform/OpenGL/fatal/exception entry. `render.substrate` exposes the counters and also proves terrain remains a 24-byte position/UV/layer/light-color layout without per-vertex block ID, mid-UV, or tangent; terrain material IDs, the multi-array atlas adapter, actual hurt overlay, generic item/armor scopes, and the broader property/uniform/independent-pack gates remain open. | `IrisDrawState`, `IrisIdMaps`, `IrisHeldItemsFrameState`, `IrisShaderPackPlanner`, `IrisWorldShaderPipeline`, `ShaderPipelineRegistry`, `FogManager`, `EntityDrawer`, `ChunkRenderer`, `HeldItemRenderer`, OpenGL blend state, focused tests, `render.substrate`, the `iris-reference` fixture, pinned Iris 1.7.2 bytecode, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The entity-overlay and existing nested-item gaps in the preceding dynamic-state row are superseded. Pinned 1.20.4 overlay-texture and Iris `EntityPatcher` behavior resolves ordinary hurt/death to exact red alpha `77/255`, retains the quantized white-progress curve, and sends the immutable value with every physical living-entity feature. Built-in skeletal/player shaders and the selected reference program apply the same overlay mix; the former host damage-tint interpolation was removed to prevent a selected program from applying red twice. Generic world/display items publish nested current-item identity, and every Gecko armor entry adds its equipped item without losing the outer entity. Uniform setters now activate/select the current host program before testing the selected bridge's accepted uniforms; this prevents state left by a preceding feature from uploading `entityColor` into a shadow program that optimized it out. A focused cross-feature regression, pure/producer tests, and the full Java 17 unit/integration gate pass. Live fingerprint `94b90775...759b` sustained 9,573 frames with 926,050 selected main and 926,050 selected shadow skeletal binds, all five terrain routes, empty fallbacks, and no post-start/reload shader, uniform, `GL_INVALID`, or fatal render entry. Subsequent rows close the entity-flame, vanilla-armor, and creeper white-progress producer gaps. | `ShaderUniform.upload`, `IrisEntityOverlay`, `IrisDrawState`, `EntityDrawer.irisDrawState`, `SkeletalFeature`, `PlayerModel`, `GeckoLibArmorFeature`, `RendererPipelineTest`, built-in/reference entity fragment shaders, focused tests, pinned bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Entity fire now has an exact retained producer rather than a fabricated draw-state alias. Pinned Minecraft 1.20.4 bytecode supplies the width ×1.4 scale, normalized-height layer count, 0.45 vertical step, 1.4 quad height, 0.9 per-layer width decay, 0.03 depth step, alternating `fire_0`/`fire_1` sheets, and two-layer UV flip. Pinned Iris 1.7.2 supplies the temporary `minecraft:entity_flame` identity. The common entity feature is state-gated, fullbright, camera-facing, and casts through the auxiliary view; built-in and selected programs share the `POSITION_TEXTURE/ENTITY_FLAME` contract. The reference pack assigns entity ID 10 and also carries block/flashing-block bridges in the entity family because those retained features can execute under entity semantics. Focused geometry/planner/draw-state tests and the full Java 17 unit/integration gate passed. Live fingerprint `92fbf1e7...e734` compiled 71 main and seven shadow variants; the final restored-after-use sheep canary on client PID 46739 produced 2,120 main plus 2,120 shadow flame selections and 2,120 main `entity=10` binds with an empty fallback map, 20 textures, seven FBOs, 122 programs, zero shader objects, and no leases. | `EntityFlameGeometry`, `EntityFlameFeature`, `EntityFlameShader`, `EntityDrawer`, `SceneStateAbi.ENTITY_FLAME`, `IrisWorldShaderPipeline`, the `iris-reference` fixture, `render.prepare-entity-flame`, focused tests, pinned bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The earlier non-Gecko armor gap is superseded. Vanilla humanoid armor now uses lazy-baked outer/inner retained meshes, copies the evaluated player/humanoid pose by transform name, masks geometry by equipment slot, and routes its physical base through the existing `PLAYER_SKELETAL/PLAYER` main and shadow contracts. Leather overlays, vanilla paletted trims, and enchanted glint are equal-depth non-caster decorations; every layer carries its equipped item ID through nested Iris draw state. Palette/path/slot/pose tests plus the full Java 17 unit/integration suites pass. Live fingerprint `83337e85...5a986` reported four retained base and decoration entries, selected ordinary player, armor-glint, and shadow programs, carried iron armor IDs 20–23 through both linked main programs, retained an empty fallback map, and restored the canary's equipment and camera state. A separate shader reload exposed the only terrain-list mutator that did not hold `VisibleMeshes.lock`; `clear()` now shares the submission/mutation lock, and the hot-reloaded joined client survived ten consecutive explicit shader reloads with stable readiness, all five terrain routes advancing, empty fallbacks, and the 20/7/122/0 texture/framebuffer/program/shader-object baseline. | `VanillaArmorFeature`, `VanillaArmorModels`, `VanillaArmorTextures`, `PalettedTexturePermutation`, `TransformInstance.copyRenderedPoseFrom`, `VisibleMeshes.clear`, `render.prepare-vanilla-armor`, focused tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The terrain material gap recorded above is superseded. Built-in and Sodium-owned terrain now share a generation-safe 84-byte adapter ABI with position, texture coordinate/array layer, packed light/color, signed-short-range shader block ID and render type, quad mid-UV, tangent/handedness, normal, and center-relative `midBlock` plus luminance. The resolver snapshots the selected shader fingerprint and ID maps during meshing; successful shader-generation changes invalidate terrain, and the loading queue rejects retired-generation meshes before GPU publication. Blocks, fluids, and world text emit complete quad metadata. This is semantic compatibility with pinned Iris 1.7.2 `XHFPTerrainVertex`, intentionally expressed through Minosoft's float-oriented VAO rather than claiming its compact 40-byte encoding. Focused tests and the full Java 17 unit/integration gate passed. A transactional live reload selected fingerprint `0b512ef8...f984`; frame 5,435 reported all nine terrain semantics and all five main/shadow routes through the 84-byte layout, the stable 30-pass/56+6-program graph, an empty fallback map, 20 textures, seven framebuffers, 105 programs, zero live shaders, and no post-start/reload shader, uniform, OpenGL, or fatal render entry. The multi-array atlas adapter is now the earliest open Iris geometry boundary. | `IrisTerrainMaterial`, `IrisTerrainQuad`, `ChunkMeshBuilder`, `ChunkMesher`, `SolidSectionMesher`, `FluidSectionMesher`, `WorldCharConsumer`, `MeshLoadingQueue`, `IrisCompatibilityAdapter`, `TerrainContracts`, focused tests, pinned Iris 1.7.2 bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The multi-array texture-size boundary recorded above now has an explicit transformed-pack contract. Pinned Iris 1.7.2 makes `atlasSize` depend on a bound Minecraft atlas and `gtextureSize` on texture unit zero; neither is a valid draw-wide value for Minosoft's per-vertex array selection. Annotated stages are rewritten to index `uTextureSizes[16]` with their actual array variable, while raw or ambiguous uses fail before publication. OpenGL static, dynamic, and font stores expose exact physical dimensions, captured once per frame and uploaded only once per linked program per frame; diagnostics report both the slot table and upload count. Focused transformation/state tests and the full Java 17 unit/integration gate passed. Live fingerprint `9cc3c206...fc1a2` reported dynamic 64, font 1024, static 16–2048 buckets, 960 cached element uploads over ten post-reload frames, all five terrain/shadow routes, the 30-pass/56+6-program graph, empty fallbacks, 20 textures, seven FBOs, 105 programs, zero shader objects, and no render fault. This establishes the internal adapter but does not claim raw third-party shader source support before the broader vertex/sampler transformer and independent-pack gate. | `IrisShaderPackPlanner.transformTextureArraySizes`, `IrisTextureArrayState`, `TextureManager.shaderTextureSizes`, OpenGL texture arrays, `IrisWorldShaderPipeline`, `render.substrate`, focused tests, pinned Iris `CommonUniforms` bytecode, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | LabPBR `_n` and `_s` companions share fixed vertical pages with each static diffuse array layer, retain neutral fallbacks, and cross transformed `texture`, LOD, gradient, fetch, and size calls without consuming more sampler units. Pinned Iris 1.7.2 companion behavior is retained: complete static or animated frame grids scale to the diffuse frame size with nearest/bilinear selection, companion metadata owns its frame order/timing/interpolation, animated diffuse total time synchronizes companions, and static diffuse textures allow independent companion animation. Candidate animation registrations publish transactionally with texture generations. Pack-facing UVs now remain logical one-page coordinates across modern and legacy terrain, scene, sky, shadow, particle, hand, weather, damaged-block, glint, beacon, and overlay adapters; static sampling maps back to the physical page, scales explicit gradients, preserves logical `textureSize`/`texelFetch`, and repeats within one material page while dynamic/font arrays remain unchanged. The vertical layout is required on the validated Apple backend: the 16×16 bucket alone contains 1,841 layers, so three layer ranges would require 5,523 layers against `GL_MAX_ARRAY_TEXTURE_LAYERS=2048`. Focused material/timeline/transformer/fallback tests, the full Java 17 unit suite, integration-source compilation, and the untouched Complementary contract pass. A rebuilt managed `RP_MODE=1;SHADOW_QUALITY=1` client retained 51+6 routes, all eight fullscreen stages and five terrain/shadow routes, empty rejection/fallback maps, the nine-semantic 84-byte terrain ABI, and the 42/7/103/0 resource baseline. The managed seven-file `iris-labpbr-render` resource fixture now adds synchronized two-frame diffuse/normal/specular sand. At frame 1,219 diagnostics reported exactly two authored companion channels, 2,438 advances and 2,438 uploads, and selected the expected `_n`/`_s` resources; a fixed 256×128 framebuffer region alternated between approximately 0.622 and 0.637 luminance in the authored phase pattern. This proves visible synchronized material phases plus real companion advancement/upload; an isolated companion-only visual response, complete tangent parity, linear page-edge filtering if the static store gains linear filtering, and another driver remain open. | `LabPbrCompanionTextures`, `TextureAnimation`, `SpriteAnimator`, `StaticTextureArray`, `OpenGlTextureArray`, `IrisLegacyShaderTransformer`, `IrisLabPbrRenderFixtureTest`, `render.substrate.materialAnimations`, focused tests, pinned Iris `AtlasPBRLoader`/`PBRAtlasTexture`/`PBRSpriteContents` bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Fluid top faces now transform their logical UVs into the selected static texture's physical diffuse page, matching side and bottom faces. Before this fix, the LabPBR vertical layout made native oceans sample diffuse, normal, and specular pages as dense blue/black radial stripes; a matched shader-disabled capture after hot reload removed them. Texture-array layer varyings are flat across direct and geometry-mediated rasterization so a primitive cannot interpolate into neighboring array layers. | `FluidSectionMesher.transformFluidUv`, `animation.glsl`, `FluidTextureCoordinateTest`, `TextureLayerInterpolationTest`, and the [temporal-stability follow-up](../evidence/2026-07-29-complementary-temporal-stability.md#artifact-triage-and-follow-up-fixes). |
| Verified | The Complementary rectangle-like “double rendering” artifact was particle presentation, not a second scene owner. Independent entity/particle suppression reproduced the artifact only with particles. The point-to-quad bridge derived world-space billboard axes from the projection-combined matrix, making size depend on FOV/aspect ratio; billboards now use the view rotation basis. The immutable shader plan also implements Iris 1.7.2 `alphaTest.<program>` directives as typed final-attachment-zero tests, including `off`/`false`, all eight comparison functions, tolerant malformed-entry handling, resolved-fallback inheritance, and the particle `GREATER 0.1` draw-state default. This supersedes the temporary sample-before-pack particle wrapper and applies the test after the authored fragment exactly where Iris's common transformer does. Diagnostics expose every effective program test. Focused tests and the real Complementary external-pack gate pass; a live generation compiled eight authored overrides plus the particle default, selected 51 main/six shadow routes with empty fallback maps, and contained no oversized or dark particle rectangles. Page-edge filtering and broader backend coverage remain open. | `ParticleRenderer.particleBillboardAxes`, `IrisAlphaTest`, `IrisShaderPackPlanner.alphaTestOverrides`, `IrisLegacyShaderTransformer.alphaTest`, `ParticleBillboardAxesTest`, planner/transformer tests, pinned Iris `ShaderProperties`/`ProgramDirectives`/`ShaderCreator`/`CommonTransformer`/`ShaderKey` bytecode, `render.substrate.shaderPrograms.alphaTests`, `visual.prepare-reference`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Retained cuboid producers derive normalized tangent direction and mirrored-UV handedness from their emitted positions and UV gradients. Generic/lightmapped skeletal, player/arm, block-feature, and held-item layouts retain that float4; the modern entity, block, and hand bridges transform it with the owning bone/model matrix and feed it into the LabPBR basis. Degenerate UVs receive a bounded orthonormal fallback, while non-cuboid ABIs retain their explicitly neutral normal-derived basis. Focused direction/handedness and transformer-layout tests, the untouched Complementary `RP_MODE=3;SHADOW_QUALITY=1` planner gate, the managed LabPBR fixture, and a hot-reloaded Apple OpenGL run pass. The live generation compiled 51 main and six shadow routes, actively selected every affected producer family with empty rejection/fallback maps, and held the stable 42/7/103/0 texture/framebuffer/program/shader ledger. | `SkeletalMeshUtil.faceTangent`, `SkeletalMesh`, `PlayerModelMeshBuilder`, `BlockMeshBuilder`, `IrisLegacyShaderTransformer`, focused tests, `IrisLabPbrRenderFixtureTest`, `render.substrate`, checked framebuffer capture, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The legacy default block-ID gap in the dynamic-state row is superseded. When `block.properties` is absent, the planner installs Iris 1.7.2's exact ordered 100-rule legacy table, including color/wood expansions and the pinned signed `-123` emerald quirk. Any present block map, including an empty file, replaces the fallback. Planner tests pin the table and replacement semantics; a real-registry integration test proves representative values reach the 84-byte terrain material ABI. Full unit/integration validation and an explicit live shader reload retained all five terrain routes, empty fallbacks, stable 20/7/122/0 GPU counts, and no render fault. | `IrisLegacyBlockIds`, `IrisShaderPackPlanner.parseBlockIdRules`, `IrisTerrainMaterialResolver`, focused unit/integration tests, pinned Iris `IdMap`/`LegacyIdMap` bytecode, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The remaining Minecraft 1.20.4 nonzero white-overlay producer is closed. A mapped-renderer scan found only creepers overriding the overlay animation counter; `CreeperFuseAnimation` now implements the exact interpolated `/28`, ten-step blink, and `0.5..1.0` clamp contract from synchronized client fuse state. All built-in and selected Iris living features consume the same quantized `entityColor`. Focused and real-entity integration tests plus the full Java 17 unit/integration gate pass. A restored client-only canary proved `whiteOverlayProgress=0.71428573` reached linked opaque and translucent entity programs as `rgba=255,255,255,128`; explicit shader reload retained all five terrain routes, empty fallbacks, and stable 20/7/122/0 GPU counts with no render fault. | `CreeperFuseAnimation`, `LivingEntity.whiteOverlayProgress`, `IrisEntityOverlay`, `EntityDrawerTest`, `render.prepare-creeper-overlay`, `render.substrate.shaderPrograms.entityColorBinds`, pinned mapped Minecraft bytecode, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The pinned Iris 1.7.2 world-information, player-direction, selected-block, cloud-time, and reachable output-color inputs now come from one immutable frame snapshot. Server dimension data retains `logical_height` and `has_ceiling`; Iris programs and custom expressions receive minimum Y, cloud plane, physical/logical height, ceiling/skylight, ambient light, current camera look, render-interpolated living-body direction, the renderer cloud clock, sRGB output ordinal, and the exact eligible outline target's material ID/camera-relative center or Iris sentinels. Material property files are preprocessed with effective options and environment before parsing, matching pinned Iris and preserving Complementary's modern `MC_VERSION` branches. A live canary resolved sand to Complementary ID 10232, restored the camera, proved the absent sentinel, and retained 51+6 routes, empty rejection/fallback maps, and the stable 40/7/103/0 resource baseline across shader generation 2→3. Distant Horizons and non-Apple compute/image/SSBO acceptance remain open. | `DimensionProperties`, `BlockOutlineRenderer.irisSelectedTarget`, `IrisWorldInfoFrameState`, `IrisSelectedBlockFrameState`, `IrisFrameStateClock`, `IrisShaderPackPlanner`, `IrisWorldShaderPipeline`, focused dimension/frame/planner tests, pinned Iris bytecode, managed Complementary sources, `render.substrate.frameInputValues`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Iris dimension replacement now completes before the frame acquires its shader-pipeline lease, so candidate compilation cannot accidentally route through the prior pinned generation. Modern fullscreen vertex transformation also synthesizes referenced core fog declarations, closing Complementary's End `deferred1` compile gap. One uninterrupted Complementary Unbound r5.8.1 cycle published `world0` → `world-1` → `world1` → `world0` as graph generations 18–21 and shader generations 3–6. Each checkpoint had empty rejection/fallback maps, zero active/retired leases, and the same 40-texture/7-framebuffer/111-program/0-shader live ledger while created/deleted counts advanced. The exact Overworld pose was restored and the managed client remains running. | `RendererPipeline.draw`, `IrisCompatibilityAdapter`, `IrisLegacyShaderTransformer`, focused transformer test, `world.teleport-player`, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Pinned Iris 1.7.2 entity and block-entity shadow fallbacks are executable. General entity shadows include the player, while `shadowPlayer=true` adds only the player when general entities are disabled. General block-entity shadows include emitters, while `shadowLightBlockEntities=true` adds only retained block states with nonzero luminance when general block entities are disabled. The graph schedules one class layer and filters at physical submission, so neither subset duplicates preparation or main-view draws. | `IrisShadowDirectives`, `RendererPipeline`, `EntityDrawer`, `ChunkRenderer`, focused planner/graph tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Pinned Iris 1.7.2 default-true `shadowTranslucent` terrain is now executable and supersedes the earlier aggregate row's rejection statement. The shadow graph draws opaque/cutout terrain and physical entity casters, snapshots `shadowtex0` into `shadowtex1`, then submits translucent terrain through the generic shadow fallback. Diagnostics count that shadow snapshot separately. A managed Complementary generation selected and submitted `shadow/translucent/shadow` against 90 visible translucent meshes/201,288 vertices with empty rejection/fallback ledgers and the stable 40-texture/7-framebuffer/111-program/0-shader baseline. | `IrisShadowDirectives`, `IrisProgramFallbacks.shadow`, `RendererPipeline`, `IrisOpenGlRenderTargets.depthSnapshotBuffers`, `WorldShaderPipelineDiagnostics.depthSnapshots`, focused tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The remaining pinned shadow producer fallbacks are retained and independently executable. General entity shadows include players; default-false `shadowPlayer` adds a player-only pass when general entities are disabled. General block-entity shadows include emitters; default-false `shadowLightBlockEntities` adds only retained states with positive luminance when general block entities are disabled. The graph keeps one pass when either corresponding choice is on, and physical feature/block-entity loops filter before submission without replaying preparation or altering the main view. First-person local-player suppression is now main-view-only, so the same retained body remains eligible for an auxiliary player shadow without duplicating it in the camera view. The focused pack selected and physically submitted only `ENTITIES/ENTITY/shadow/PLAYER_SKELETAL/PLAYER` in player mode, then only `BLOCK_ENTITIES/ENTITY/shadow/SKELETAL/SKELETAL_LIGHTMAP` for a real ender chest in light-emitter mode. The canary, official Complementary archive, exact options, resource ledger, and player pose were restored. | `EntityRenderFeature.mainViewEnabled`, `FeatureManager.collectShadow`, `LocalPlayerRenderer`, `IrisShadowDirectives.allowsEntity`, `IrisShadowDirectives.allowsBlockEntity`, `EntityDrawer.drawShadowCasters`, `ChunkRenderer.drawBlockEntities`, `iris-block-entity-shadow`, focused tests, live `render.substrate`, pinned Iris source, and the [fallback-only shadow checkpoint](../evidence/2026-07-26-iris-render-pipeline-support.md#fallback-only-player-and-light-block-entity-checkpoints). |
| Verified | Complementary's authored shadow projection now reaches frame-pinned matrices and culling volumes instead of being clamped to the host camera. The plan retains modern and legacy `SHADOWRES`/`SHADOWHPL`/`SHADOWFOV` declarations with pinned precedence, `voxelDistance`, and exact `shadow.culling` default/advanced/distance/reversed choices. Orthographic/perspective projection uses Iris semantics; the light view follows its exact baseline translate/X-Z-X rotation order, float-narrowed interval stabilization, and render-origin correction. Advanced culling derives the six camera planes and extrudes back/edge planes toward the world-space shadow light; reversed mode combines the voxel core, outer distance box, and advanced fringe. Default mode selects distance culling only when the primary shadow geometry uses voxel images. Authored distance multipliers gate terrain sections and independently gate entity/block-entity bounds at Complementary's 192/24 blocks. The auxiliary collector traverses unique loaded sections without main occlusion and prepares a separate bounded entity-shadow set without duplicating main draws. A four-block TNT selected both routes, a naturally out-of-frustum zombie inside 24 blocks still selected the shadow skeletal route, and live Complementary defaulted to advanced terrain/entity culling with all routes/snapshots advancing, empty rejection/fallback maps, and stable resources. Loading beyond host distance, perspective-specific checked pixels, and cross-driver references remain open. | `IrisShadowDirectives`, `IrisShaderPackPlanner`, `IrisFrameState`, `IrisShadowCullingVolume`, `irisShadowProjection`, `irisShadowModelView`, `LoadedMeshes`, `ChunkRenderer`, `EntitiesRenderer`, `EntityDrawer`, focused tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Split Gecko block-entity render layers now remain complete in the Iris shadow view. Pinned Iris renders block entities and flushes every retained render type before the shadow depth snapshot, while explicitly forbidding a later translucent-entity flush and cancelling beacon beams. `BlockEntityRenderer.drawShadow` therefore performs the normal base draw and permits only an explicit renderer opt-in to append its split translucent/additive layers inside that same submission. Gecko renderers opt in when their baked model contains such layers; beacon and ordinary renderers retain the default base-only behavior. `ChunkRenderer` applies the existing block identity, luminance, distance, and light-frustum gates once, without another graph pass, another animation update, or a duplicate main draw. Focused draw-order tests and the untouched Complementary planner gate pass. Complementary's authored default disables block-entity shadows, so the current live run proves the unchanged main `gbuffers_block` route and stable resources rather than fabricating a positive shadow draw against the pack's choice. | `BlockEntityRenderer.drawShadow`, `GeckoLibBlockEntityRenderer.castsTranslucentShadow`, `ChunkRenderer.drawBlockEntities`, focused tests, pinned Iris `ShadowRenderer`/`MixinBeaconRenderer`, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | GUI mesh indices are counted per complete four-vertex quad. A paged JEI recipe screen exposed and now guards the former vertex-count/index-count mismatch; 256 vertices produce 64 quads, and incomplete quads are rejected. | `GuiMeshBuilder.quadCount`, `GuiMeshBuilderTest`, and [Iris/JEI activation evidence](../evidence/2026-07-23-iris-jei-activation.md). |
| Verified | Adapted mod JAR assets can be mounted into the normal per-session priority asset stack, and property-bearing Tech Reborn blockstates reach model loading. | `ExternalAssetProviders`, `AssetsLoader`, `FabricSessionContentBridge`, and [Tech Reborn world-generation evidence](../evidence/2026-07-21-tech-reborn-worldgen.md). |
| Verified | Verified external resource-pack ZIPs participate in the same session priority stack; a live debug capture showed Faithful 64x replacements after a supervised client reload. | `resourcepacks/*.pw.toml`, `AssetsLoader`, and [resource-pack evidence](../evidence/2026-07-22-resource-pack-stack.md). |
| Verified | Moon phase affects night-sky appearance but cannot zero the terrain lightmap; a new moon retains a bounded sky-visibility floor. | `NightLighting`, `NormalLightmapUpdater`, `SkyboxColor`, and `NightLightingTest`. |
| Verified | The pause menu exposes a Minosoft-native Lighting screen for persisted gamma, fullbright, and ambient-occlusion changes; gamma changes explicitly invalidate the live lightmap. Brightness uses the shared discrete slider rather than separate step buttons. New profiles and Reset use Minecraft 1.20.4's pinned `0.5` default instead of the former zero-brightness value, which also keeps Iris `screenBrightness` inputs at upstream defaults. | `LightC`, `LightingMenu`, `LightingControls`, `Lightmap.init`, focused control tests, pinned Minecraft 1.20.4 `GameOptions` bytecode, and [stepped-slider evidence](../evidence/2026-07-23-stepped-slider.md). |
| Verified | A presentation-only player light prevents fully black nearby surfaces without mutating world light: it peaks at a configurable 15% by default, falls quadratically to zero over six blocks, and is evaluated per fragment for terrain, breaking overlays, and lightmapped entities. The Lighting menu slider adjusts it from Off through 30% in five-percent steps. | `PlayerLightShader`, `PlayerLightFalloff`, `player_light.glsl`, focused tests, [player-light evidence](../evidence/2026-07-23-player-light.md), and [stepped-slider evidence](../evidence/2026-07-23-stepped-slider.md). |
| Verified | Pause → Audio exposes the existing OpenAL engine through profile-backed live enable/volume controls, actual initialization status, normal-path test output, sound-family policies, stop-all, and a restart-qualified startup policy. Master volume uses the shared discrete slider and reaches listener `AL_GAIN`; layered sound indices preserve vanilla events while honoring pack append/replace semantics. Both final pages fit the live 1800×1000 viewport. | `AudioMenu`, `AudioAdvancedMenu`, `AudioControls`, `SoundIndexMerger`, focused tests, [audio-menu evidence](../evidence/2026-07-22-audio-menu.md), and [stepped-slider evidence](../evidence/2026-07-23-stepped-slider.md). |
| Verified | Pause → Mod settings exposes the active pack's shared compatibility catalog and runtime diagnostics, including global FPS context and attributed invocation timing at owned graphics hooks. | `FabricModSettingsMenu`, `FabricModDiagnosticsMenu`, `FabricModDiagnostics`, and [2026-07-22 catalog/diagnostics evidence](../evidence/2026-07-22-fabric-catalog-diagnostics.md). |
| Verified | Native configuration screens can adapt typed Boolean, text, stepped, and cycle entries into staged forms with validation, dependency-driven disabled states, restart markers, and reset/apply/cancel actions. Variable-height panels render and tick intersecting rows and CPU-clip textured, item, solid, and text quads at the viewport. Live visual acceptance remains. | `SettingsFormMenu`, `CycleSelectorElement`, `ClippedScrollPanelElement`, `ClippedGuiVertexConsumer`, focused tests, and [settings-form evidence](../evidence/2026-07-24-settings-forms.md). |
| Verified | Categories/tabs, focused filtering, hover descriptions, virtualized grids, status/modal presentation, map canvases, and tank/energy/progress gauges reuse the retained GUI and shared CPU clip path. Iris define overrides are applied before candidate compilation; JEI ingredient/recipe cells retain item tooltip behavior. Live visual acceptance remains. | `TabBarElement`, `SettingsFilter`, `ClippedVirtualGridElement`, `MapCanvasElement`, `GaugeElement`, `IrisShaderPackPlanner`, `JeiCompatibilityAdapter`, focused tests, and [Fabric UI evidence](../evidence/2026-07-24-fabric-ui-framework.md). |
| Verified | Manual shader and texture reloads execute on the render queue and publish owned Fabric prepare/apply/complete/failed lifecycle events. OpenGL shader reload prepares and links before swapping. Dynamic texture reload/resize and static content texture refresh prepare complete candidate arrays, preserve the active handle on failure, and retire the replaced handle after publication. Invalid Iris reference-pack reloads preserve the active program/target generation through twenty failure/recovery cycles; a repeated typed real-GL texture baseline remains open. | `ReloadCommand`, `FabricResourceReloadEvents`, `OpenGlNativeShader`, `OpenGlDynamicTextureArray`, `StaticTextureArrayUpdate`, `ShaderPipelineRegistry`, focused tests, and [R0–R7 checkpoint](../evidence/2026-07-24-render-substrate-r0-r7.md). |
| Verified | Entity-feature mesh disposal is state-aware and idempotent: immediate shutdown does not enqueue a duplicate GPU unload, and queued replacement cleanup rechecks mesh state on the render thread. | `MeshedFeature`, focused entity-rendering tests, live post-reload log sampling, and [entity mesh unload evidence](../evidence/2026-07-22-entity-mesh-unload.md). |
| Verified | OpenGL texture shaders initialize every active sampler-array slot to a valid 2D-array unit; dynamic arrays grow for high-resolution pack skins, and font arrays allocate independent width/height maxima. Faithful 64x no longer produces Apple's unloadable-array warning or `GL_INVALID_VALUE`. | `OpenGlTextureManager`, `OpenGlDynamicTextureArray`, `OpenGlFontTextureArray`, `OpenGlTextureSizing`, focused tests, and [resource-pack OpenGL evidence](../evidence/2026-07-22-resource-pack-opengl-audio.md). |
| Verified | Positional audio refreshes the listener from the current camera before attenuation and reports requested, resolved, rejected, buffer-ready, and source-start stages. A harvested stone started at its block-center world coordinate. | `AudioPlayer`, client debug state, and [positional-audio evidence](../evidence/2026-07-22-resource-pack-opengl-audio.md). |
| Verified | Mining progress plays the block sound group's `hit` event at block center with a frame-rate-independent 200 ms cadence; remote break-animation updates use the same position path, while completion retains the distinct `destroy` sound. | `BlockHitAudio`, `BlockHitCadence`, `SurvivalDigger`, `BlockBreakAnimationS2CP`, focused tests, and [harvesting evidence](../evidence/2026-07-22-resource-pack-opengl-audio.md). |
| Verified | The first-person arm consumes player-owned 200 ms swing progress and applies a bounded hand transform during harvesting; framebuffer comparison shows the hand displaced inward/downward while the target remains in its partial-break state. | `ArmSwingState`, `ArmRenderer`, focused tests, and [harvesting evidence](../evidence/2026-07-22-resource-pack-opengl-audio.md). |
| Verified | Flat generated items retain visible screen area in either first-person hand. A hand-local corrective yaw compensates for the standard ±90-degree display rotation until generated item meshes gain real thickness; 3D block-item poses remain unchanged. | `FirstPersonItemTransform`, `HeldItemRenderer`, and `FirstPersonItemTransformTest`. |
| Verified | First-person hand rendering preserves world depth and uses the pinned Iris-style `0.125` clip-depth projection scale instead of clearing the complete depth buffer. The hand remains in front while later overlays and shader post-processing retain the opaque/translucent depth history. | `ArmRenderer.handProjection`, `FirstPersonItemTransformTest`, pinned Iris 1.7.2 hand bytecode, live native/Complementary captures, and the [temporal-stability follow-up](../evidence/2026-07-29-complementary-temporal-stability.md#artifact-triage-and-follow-up-fixes). |
| Verified | Chest and shulker-box item families use their resolved entity textures for GUI/world-item rendering. All 61 vanilla 1.20.4 `builtin/entity` block items are classified: 20 have precise box-family renderers and 41 receive a visible particle-texture fallback with structured audit output. The crafted shield is audited separately and receives the same safe fallback. Entity-backed block transitions invalidate section meshes even without a normal baked model, so a live chest harvest/pickup/place cycle remained visible without restarting the client. | `ChestItemRender`, `ShulkerBoxItemRender`, `ItemLoader`, `ItemRenderUtil`, `ChunkRendererChangeListener`, focused tests, and [chest/generalized block-item evidence](../evidence/2026-07-23-chest-rendering.md). |
| Verified | A block item's dedicated generated/layered item model replaces its world-block fallback, while ordinary inherited block models remain 3D. Automated coverage distinguishes ladder from oak planks; live debug-pipe acceptance verified the Faithful ladder result centered in its crafting slot. | `ItemLoader`, `RenderTestLoader`, and [generated block-item evidence](../evidence/2026-07-23-generated-block-item-rendering.md). |
| Verified | Creative player inventories render a 9×7 registry-backed catalog beside the normal container. Live capture and input acceptance covered page 1→2 scrolling and creative stack pickup while preserving the inventory and adapted-mod controls. | `CreativeItemCatalogElement`, `InventoryScreen`, `CreativeCatalogPagerTest`, and [creative catalog evidence](../evidence/2026-07-23-creative-catalog-gamemode-aliases.md). |
| Verified | The fullscreen Creative workspace proportionally expands a bounded item/creature/recipe catalog while preserving fixed vanilla slot geometry. Creature previews retain raw production quads/bone IDs, evaluate real clips at 20 Hz, expose neutral/clip selection, support drag rotation plus a 20-second idle orbit, and refresh exact species textures after transactional replacement. | `CreativeInventoryLayout`, `CreativeInventoryCatalogElement`, `CreativeCreatureCatalogElement`, `SkeletalPreviewPlayer`, focused tests, and [creature catalog evidence](../evidence/2026-07-26-creative-creature-catalog.md). |
| Verified | Skeletal static and animated transforms share column-vector joint order `T(pivot) R S T(-pivot)`; Gecko rotations are converted from their source convention. Gecko box UVs preserve Bedrock front/back and mirror semantics; hinged zero-thickness planes snap/overlap without changing UV size. Preview expression time advances at 20 Hz and unique normalized bone aliases bridge snake/camel case. Pinned Naturalist corrections align its remaining authored appendage/atlas defects. Sheep wool is separately inflated and state-gated, and the base body spans the leg pairs rather than projecting forward. | `TransformInstance`, `SkeletalPose.apply`, `SkeletalModelBinder`, `SkeletalPreviewPlayer`, `NaturalistCompatibilityAdapter`, `SheepRenderer`, focused transform/preview/adapter tests, and [creature catalog evidence](../evidence/2026-07-26-creative-creature-catalog.md). |
| Verified | Pause → Debug options → Debug rendering exposes the active world framebuffer's fill/line mode and the existing F3 debug HUD state with live labels and a reset. The legacy polygon key binding toggles current framebuffer state, preserving one source of truth across keyboard and menu control. | `DebugRenderingMenu`, `DebugRenderingControls`, `DebugKeyBindings`, focused tests, and [debug-rendering menu evidence](../evidence/2026-07-23-debug-rendering-menu.md). |
| Verified | Zombies use a dedicated six-part skeletal model whose left limbs mirror the populated classic-zombie UV regions. Zombie variants remain visible, and otherwise unmapped living entities fail visibly through a debug-textured humanoid renderer instead of a model-less dummy. The generic fallback is diagnostic geometry, not species-accurate modeling. | `ZombieRenderer`, `HumanoidMobRenderer`, `FallbackLivingEntityRenderer`, focused resource/integration tests, and [mob/title evidence](../evidence/2026-07-23-mob-rendering-title-menu.md). |
| Verified | The credits screen builds its scroll from original Minosoft content plus same-key local pack/mod contributions; it advances at a stable 20 Hz cadence and rebuilds only lines intersecting the viewport. | `CreditsContent`, `CreditsScreen`, focused tests, and [credits parity evidence](../evidence/2026-07-23-credits-parity.md). |
| Observed | Full Tech Reborn rendering is incomplete: Fabric/custom builtin models and at least one machine texture still report diagnostics. | Live diagnostics recorded in [Tech Reborn world-generation evidence](../evidence/2026-07-21-tech-reborn-worldgen.md). |
| Verified | OpenGL buffers, vertex arrays, textures, renderbuffers, framebuffers, shader objects, programs, and queries are tracked per context and type. Duplicate live creation and unknown deletion fail closed; allocation failures roll back candidates; render diagnostics expose live/cumulative counts; shutdown closes texture arrays, integrated framebuffers, registered shaders, and loaded/cached terrain resources while the context remains current, and warns on remaining names. Terminal terrain coverage discard does not reacquire its already-closed provider generation; exhaustive unload draining continues after individual failures. A built-in Apple OpenGL run with 67 region pages and 13 block entities removed the former 28-buffer/15-VAO shutdown remainder. Separate stencil renderbuffers attach to the stencil attachment point. Animated Java now has exact positive and two-phase rejected/recovery reload accounting; equivalent fixed EMF/ETF/Gecko loops remain. | `OpenGlResourceTracker`, instrumented OpenGL resource implementations, `RenderUtil.unload`, `ChunkRenderer.closeTerrainCore`, `MeshUnloadingQueue.drain`, `render.substrate`, focused tests, [terrain consolidation evidence](../evidence/2026-07-30-terrain-consolidation-start.md), and [OpenGL resource-accounting evidence](../evidence/2026-07-24-opengl-resource-accounting.md). |
| Verified | Vanilla JSON and PNG support has an explicit fidelity matrix. Declared `cullface` is distinct from boundary occlusion geometry and rotates with blockstate applies; inherited model AO survives baking per face; animation sheets honor rectangular frame dimensions, row-major grids, and object-entry `frametime` fallback. The audited 1.20.4 legacy item-override path preserves last-match ordering, fails unknown/wrong-item providers as negative infinity, and implements every registered provider. Bundle occupancy uses registry stack capacities; trim uses the fixed 1.20.4 material indices; clock and compass variants consume live world/entity targets through context-owned wrapped smoothing. First-person and GUI paths resolve live context on draw, world/display item features reselect and rebuild after threshold changes, and living model state retains remote active-hand use ticks across 1.19.4/1.20.4 fixtures. Session-selected 1.19.4 and 1.20.4 catalogs keep the latter release's brush/trim providers out of the former; older releases still use an explicit compatibility catalog pending individual audits. Later component dispatch, sampler metadata, and rendered references remain incomplete. | `ModelFace`, `SingleBlockStateApply`, `BakedFace`, `BakedModel`, `AnimationProperties`, `SpriteAnimator`, `LivingEntity`, `ItemPredicate`, `ItemPredicateCatalog`, `ItemPredicateContext`, `ItemPredicateRuntime`, `ItemFeature`, focused unit/integration tests, [content-system documentation](../../Assets.md), [item-predicate evidence](../evidence/2026-07-24-item-model-predicates.md), and [foundation evidence](../evidence/2026-07-24-content-fidelity-foundation.md). |
| Verified | The exact 1.20.4 ETF, EMF, and GeckoLib artifacts select source-native adapters without executing upstream bytecode. CEM and Gecko geometry parse into a neutral model; entity/version aliases and per-instance CEM expressions reach retained transforms. EMF's 3.0.17 public expression input names and audited numeric/easing methods are represented, observable entity/render values are live, ordered shadow/leash outputs are retained, and entity-specific aliases cover the five skeletal rigs Minosoft ships. Targeted CEM parts compose with native geometry and isolated attach roots. Matching Gecko clips update retained bones; the pinned 4.4.4 built-in easing catalog and first arguments are exact; owner-scoped custom easings reach retained transforms; and identity-scoped entity routes bind adapted multi-controller sets. Owner-scoped opaque/translucent/additive texture passes are generation-baked, predicate-driven, and token-matched to prevent replacement callbacks from driving old meshes. The Gecko facade covers predicate/concurrent-layer control, state-driven speed/easing overrides, triggerable animation preemption/base reload, typed handlers retaining particle locator/pre-effect data, owned caches, bounded raw default/play-once/hold/loop/wait/repeat queues with cross-stage delta carry, stable finished identity, explicit reset, owned custom loop decisions, compatible retained controller-state migration without keyframe replay, and generic-object managers with typed data, triggers, snapshots, and bounded instance ownership. Finalized entity/bone transforms feed native positional sound, registered particle factories with locator placement, and owner-scoped custom-instruction listeners. ETF context and material discovery include configured emissive suffixes, blink/blink2 timing, exclusions, semantic version ranges, calendar/world/client values, percent health, and bounded NBT; skeletal body/feature materials select independently, while ETF-marked player skins derive matching blink/emissive dynamic textures, all eight coat styles/lengths, moved-base edits, coat emissives, fat-coat inflation, leggings suppression, forced lower-skin opacity, legacy/controller villager noses, five textured nose layouts, removal edits, nose emissives, marker-selected animated glint masks, and profile-controlled ETF-only base transparency. `/reload content` now reparses, bakes, and uploads CEM/Gecko models plus ETF material/cache generations—including retained native entity models—on the render thread. Refreshed and new static textures keep stable shader coordinates while affected size buckets prepare candidate handles; model and texture lookups publish or roll back together, successful publication deletes replaced handles, and retained instances defer old mesh/cache/texture-slot cleanup. Dead content-owned holes are reused before append, dead trailing layers compact on the next transaction, and permanent resource-pack coordinates remain untouched. Missing assets reject the candidate. A headless fixture crosses 1.19.4 and 1.20.4. Animated Java foundations retain arbitrary cuboid item geometry, render item/block/text displays, execute a bounded selector/entity lifecycle against session-owned display/interaction passenger trees, capture local interaction packets, and resolve `execute on target`/`execute on attacker`. Failed candidate loads restore command and entity state. Broader EMF aliases, exact live semantics for all catalogued variables, absolute part and complex attachment parity, exact player-holder body-yaw dynamics, non-default shadow sampler metadata, broader non-skeletal/block-entity ETF features and configuration, Gecko binary APIs, GUI item views, exact armor fitting, dependent-mod validation, remaining Animated Java exporter semantics, repeated real-GL accounting, and visual acceptance remain partial. | `assets/model/`, `assets/datapack/`, `local/datapack/`, `SkeletalModelBinder`, `SkeletalModelComposer`, `CemExpressionVariableCatalog`, `CemExpressionManager`, `EntityTextureMaterial`, `EtfPlayerSkinProcessor`, `EtfPlayerSkinTextures`, `PlayerModel`, `PlayerModelMeshBuilder`, `GeckoLibControllerBindingRegistry`, `GeckoLibEntityModelRegistry`, `GeckoLibRenderLayerRegistry`, `GeckoLibAnimationManager`, `GeckoLibAnimatableManager`, `GeckoLibControllerSet`, `GeckoLibControllerSetSnapshot`, `GeckoLibRawAnimation`, `GeckoLibEasingRegistry`, `GeckoLibLoopTypeRegistry`, `GeckoLibControllerKeyframeEvent`, `GeckoLibRuntimeEvents`, `GeckoLibEntityEventConsumer`, `StaticTextureArrayUpdate`, `StaticTextureSlotOwnership`, `DisplayEntityRenderer`, `TextDisplayFeature`, `SkeletalLoader.reloadContentFidelity`, `ContentGenerationStore.reloadLeased`, `ContentFidelityMultiVersionTest`, `modpacks/content-fidelity/`, focused tests, [native adapter evidence](../evidence/2026-07-24-content-fidelity-native-adapters.md), and [content-system documentation](../../Assets.md). |
| Verified | EMF 3.0.17 part properties now read each retained transform's live aliased pivot, rotation, scale, visibility, and box-hidden state before expression evaluation. Ordered assignments replace those values absolutely, same-frame reads see earlier writes, unknown reads return zero, and unknown write targets fail. Rendering keeps `visible=false` subtree suppression separate from `visible_boxes=true` local-cube suppression. The transactional reload fixture proves a model-derived pivot read before and after publication. This supersedes the absolute-part-property gap in the preceding aggregate row; still-defaulted live inputs, diagnostics, and complex attachments remain partial. | `CemExpressionEvaluator`, `CemExpressionManager`, `TransformInstance`, neutral/keyframe animators, `CemExpressionEvaluatorTest`, `CemExpressionManagerTest`, `SkeletalLoaderTest`, and [native adapter evidence](../evidence/2026-07-24-content-fidelity-native-adapters.md). |
| Verified | Gecko routing now isolates entity, block-entity, item, and armor targets under the same content identity. Routed block entities retain generation-owned skeletal instances in section caches; world/item-display and first-person held items consume item routes; living renderers consume independent armor-slot routes. Controller events and baked render layers run on these paths. Content reload migrates compatible item/armor controller state and invalidates routed block-entity caches. Generic non-rendered objects now use headless typed managers and instanced/singleton caches outside this renderer boundary. This supersedes the object/block/item/armor-routing gap in the preceding aggregate row; GUI item views, exact armor-to-parent-bone fitting, dependent-mod fixtures, and visual acceptance remain. | `GeckoLibModelRouteRegistry`, `GeckoLibAnimatableManager`, `GeckoLibBlockEntityRenderer`, `ItemFeature`, `HeldItemRenderer`, `GeckoLibArmorFeature`, `EntityRendererManager.reloadContentModels`, `SkeletalLoader.reloadContentFidelity`, focused route tests, [native adapter evidence](../evidence/2026-07-24-content-fidelity-native-adapters.md), and [content-system documentation](../../Assets.md). |
| Verified | Unknown living-entity renderers consume routed Gecko geometry through the generic skeletal feature instead of requiring humanoid transforms. Read-only inspection separates the resolved route from the retained instance and counts base, selected-texture, emissive, and registered Gecko-layer vertices/passes. A live Naturalist rattlesnake retained one 252-vertex selected-texture pass with no fallback body or overlay pass. Pinned GeckoLib 4.4.4 emits all six cube faces, including zero-thickness planes, so generic joint-face deletion is not compatible. Minosoft now keeps Gecko's floored source box-UV dimensions independent from inflated vertex bounds; Naturalist's `inflate: 0.01` segments no longer stretch a `4×3×6` atlas layout to `4.02×3.02×6.02`. The seven source cubes still contain authored coplanar joint/zero-height candidates, so checked close-view seam pixels remain a separate gate. | `HumanoidMobRenderer`, `ContentModelInspectable`, `ClientDebugChannel.client.entities`, `SkeletalModelBinder`, `SkeletalElement.boxUvSize`, `CuboidUtil`, focused binder/UV and Gecko tests, and [Naturalist registry-sync evidence](../evidence/2026-07-26-naturalist-registry-sync.md). |
| Verified | EMF `render.shadow_*` and `render.leash_offset_*` outputs now cross an owner-scoped renderer bridge. Living entities build translucent terrain-conforming shadows with EMF size, opacity, distance fade, offsets, baby scaling, and the audited 32-block cap. A bounded vertical scan projects onto lit full-collision outline surfaces with light and vertical falloff; world block/chunk revisions invalidate stationary projections after terrain changes. Projected quads sample the active resource pack's vanilla shadow resource using audited inverted two-radius UVs, fixed level-zero clamped sampling, and standard source-alpha blending. Leashes use distinct session-owned holder state, additive local EMF offsets rotated with the mob anchor, vanilla generic/knot/player-pose holder formulas, quadratic sag, alternating colors, and two crossed 24-segment ribbons. Mob skeletal roots and leash anchors consume one previous/current vanilla body-control pose. Ribbon vertices independently interpolate block/sky endpoint light through a dedicated packed-light mesh and the shared lightmap. Mesh replacement, light-change replacement, mutation replacement, and unload are covered on the dummy GPU. This supersedes the shadow/leash-consumption, coarse leash-geometry, mob body-yaw interpolation, and leash lightmap gaps in the aggregate row; exact player-holder body-yaw dynamics, non-default shadow sampler-metadata overrides, and rendered reference acceptance remain. | `EntityBodyRotation`, `EntityRenderInfo`, `EntityRenderEffects`, `EntityShadowProjector`, `EntityShadowFeature`, `EntityLeashProjector`, `EntityLeashFeature`, `LightColorMeshBuilder`, `LightColorShader`, `EntityRenderFeatures.SHADOW_TEXTURE`, `ShaderManager.entityShadowTextureShader`, `ShaderManager.entityLeashShader`, `SkeletalFeature`, `World.blockRevision`, `FabricBlockMutationEvents`, `EntityAttachment`, `EntityAttachS2CP`, `EntityBodyRotationTest`, `EntityRenderEffectsTest`, `EntityLeashProjectorTest`, `EntityRenderEffectFeatureTest`, `AnimationPropertiesTest`, `LocalDisplayEntityFactoryTest`, and [native adapter evidence](../evidence/2026-07-24-content-fidelity-native-adapters.md). |
| Verified | Animated Java's pinned 1.10.2 compiler command path has upstream-shaped headless coverage for entity-backed macros, entity-data predicates and sources, score/function conditions, nested `execute … run return` ownership, packet-driven interaction reward dispatch, and exact signed four-word UUID conversion. The reduced lifecycle fixture includes the exporter's tween-guard return form. Separately, exact Blockbench 5.1.4 plus the official Animated Java 1.10.2 plugin exported the upstream 1.20.4 minimal armor stand without shims or export errors; all 122 output files are pinned by manifest. Minosoft passes manifest/model discovery plus the unmodified load/summon/walk/remove lifecycle. Eight consecutive runtime replacements retain root/passenger object identity, IDs, UUIDs, hierarchy, and advancing animation; a rejected load rolls back and recovers, and every retired CPU generation closes exactly once after its lease moves. The exact output also added native coverage for trailing-comma SNBT, append-created lists, missing data-source command failure, and atomic function-macro preflight. This supersedes the data-manager/UUID/interaction-callback, exporter-generation, first headless runtime-acceptance, and headless reload/CPU-cleanup gaps; remote-server behavior, rendered references, and repeated real-GPU reload remain. | `SnbtParser`, `DataPackFunctionRuntime`, `SessionDataPackRuntime`, `ContentGenerationStore`, `LocalDataPackCommandAuthority`, `LocalConnection`, `SnbtParserTest`, `DataPackFunctionRuntimeTest`, `AnimatedJavaExportFixtureTest`, `LocalDisplayEntityFactoryTest`, the fingerprinted fixtures, [Blockbench export evidence](../evidence/2026-07-24-animated-java-blockbench-export.md), and [native adapter evidence](../evidence/2026-07-24-content-fidelity-native-adapters.md). |
| Verified | Item, block, and text displays now share mapped-1.20.4 range rejection and metadata-derived visibility bounds. Retained interpolation covers transforms, absolute terrain-projected shadow radius/strength, text background/opacity, negative start deltas, and capped teleport pose smoothing with shortest-path yaw. Entity flag `0x40` independently enables the canonical texture-mask/composite outline pass; display overrides, team color, and white fallback select color. Occluded or invisible glowing geometry remains outline-eligible without leaking into normal layers. Block/item/text, EMF/ETF skeletal, Gecko layer/armor, and emissive geometry are scoped in; names, hitboxes, shadows, and leashes are scoped out. The registry-backed display fixture and exact unmodified Blockbench export pass together, and a real-GL launch proved shader/FBO construction, graph placement, three reloads with stable texture/framebuffer/program counts, and endpoint cleanup. Rendered references, remote-server behavior, and a quiescent full real-GPU baseline remain. | `DisplayEntity`, `DisplayTransformationInterpolator`, `DisplayShadowInterpolator`, `DisplayTextStyleInterpolator`, `DisplayPoseInterpolator`, `DisplayEntityRenderer`, `EntityOutlineColor`, `EntityOutlineFeature`, `EntityOutlineRenderer`, `FeatureManager`, `EntityDrawer`, scoped render features/shaders, focused unit/integration tests, and [display semantics evidence](../evidence/2026-07-24-animated-java-display-semantics.md). |
| Verified | The exact Animated Java 1.10.2 export now mounts from the managed content-fidelity fixture into normal session resource/data-pack managers and renders through `ItemDisplayEntityRenderer`/`ItemFeature` in a real-OpenGL void world. Seven independently collected display bones originally flickered at coplanar body joints because equal feature/distance comparisons inherited parallel collection order; every feature drawable now has a stable entity-identity final order. Ten repeated default-pose crops and the crop after four content generations were pixel-identical. Session asset/data-pack managers transfer to session ownership at initial commit, so retiring a content snapshot no longer removes mounted functions; disconnect/error owns final manager unload. Four summon/reload/remove cycles retained exact functions and returned the 42 fixture GPU objects to the warmed baseline family without cumulative growth. Checked default/walk references, rejected-reload GPU recovery, and remote-server behavior remain. | `Play.fixtureMounts`, `SessionAssetsCandidate`, `LocalConnection.executeDataPackFunction`, `FeatureDrawable.stableOrder`, `DrawableEntityRenderFeature.stableOrder`, `compareFeatureDrawables`, `EntityDrawOrderTest`, `PlayUtilityTest`, live capture/resource evidence, and the [Blockbench acceptance protocol](../acceptance/blockbench.md). |
| Verified | The exact Animated Java export now has platform-qualified checked real-OpenGL references: on the documented macOS/Apple M4 Max `3456x1910` target, the 550x550 default pose and static walk frame 10 match with zero tolerance, and the walk frame remains exact after production content reload. `visual.prepare-reference` clears transient overlays and disables the HUD; a deliberate pause-menu precondition still passes. Allocation stacks proved the observed buffer finalizers came from dropped cloud arrays rather than display features. Cloud arrays now unload their meshes, grid shifts release only outgoing edges, retired layers drain once, and shutdown releases active/pending layers. After entity removal and a 512-block out/back camera jump, two forced collections emitted no new GPU finalizer or double-unload warning. This supersedes the checked-reference and positive-cleanup gaps in the preceding Animated Java row; another driver, rejected-reload recovery, glowing-outline pixels, and remote-server behavior remain. | `animated-java-render-reference.json`, `animated-java-render-cleanup.json`, checked platform baselines, `visual.prepare-reference`, `CloudArray`, `CloudLayer`, `CloudRenderer`, real-GL scenario reports, forced-GC diagnostics, and the [Blockbench acceptance protocol](../acceptance/blockbench.md). |
| Verified | Animated Java rejected-reload recovery now crosses the real upload and publication rollback boundaries. Bounded `after-upload` and `after-publication` checkpoints each create then delete 16 buffers and eight vertex arrays with zero typed live delta, preserve the active generation and exact walk pixels, and leave mounted functions callable; accepted recovery then publishes normally. This supersedes the rejected-reload gap in the preceding row; another driver, glowing-outline pixels, and remote-server behavior remain. | `ContentReloadRejectionPoint`, `SkeletalLoader.reloadContentFidelity`, `ClientDebugChannel.reloadContent`, `animated-java-rejected-reload.json`, focused headless rollback assertions, real-GL scenario evidence, and the [OpenGL accounting map](../evidence/2026-07-24-opengl-resource-accounting.md). |
| Verified | EMF's raw `nbt(key,query)` expression syntax preserves escaped delimiters and resolves the current entity through ETF's bounded NBT predicate engine. ETF and EMF now share existence/inversion, integer-range, wildcard list-path, raw/string, wildcard, and bounded-regex matching; this supersedes the raw-NBT gap in the aggregate row. Exact upstream diagnostics and remaining ETF predicate families are still partial. | `SkeletalExpression`, `CemExpressionEvaluator`, `SkeletalFeature`, `EntityTextureContextFactory`, `EntityTextureConditions`, and focused expression/property tests. |
| Verified | ETF live context now covers biome tags, pack-scoped active-mod IDs, equipment keywords and item IDs, general mob variants, panda genes, llama inventory strength, horse jump and movement attributes, separate bounded entity/client-player/vehicle NBT, ordinary-entity spawner state, vanilla-client regional difficulty, and prior rule/suffix selection. Vertical block probes are requested only for catalog predicates that need them. Pinned-bytecode auditing established that `blocks` and misleadingly named `blockSpawned` both inspect the current and immediately lower block; both preserve ETF's colon-separated block-state subset checks, while solid probes consume the `solid_render`-derived flag matching `isOpaqueFullCube`. A retained native-skeletal integration reloads independent pig/saddle materials and proves exact half/closed/open blink selection, paired blink/blink2 emissives, and two geometry plus two emissive passes while the retired generation remains leased. Broader non-skeletal/block-entity features and independent variant/emissive/blink pixel references remain explicit gates. | `EntityTextureContextFactory`, `EntityTextureRegionalDifficulty`, `EntityTextureCatalog.contextKeys`, `EntityTextureSelectionCache`, `EntityTextureRuntimeEnvironment`, `SkeletalLoaderTest.native skeletal feature materials select independent ETF layers`, focused unit tests, and [native adapter evidence](../evidence/2026-07-24-content-fidelity-native-adapters.md). |
| Verified | One fixed 1.20.4 living scene now crosses ETF selection, EMF expression context, targeted native-zombie CEM composition, local data-pack summon/removal, retained rendering, transaction rejection/recovery, and real OpenGL. Pinned EMF 3.0.17 bytecode confirms `rule_index`; ETF's `textureRule` alias remains confined to ETF predicates. After the entity light interpolator settles, adjacent crops and all pre-/post-transaction references are exact at zero tolerance, showing one composed skeletal draw rather than competing body geometry. Upload and publication rejection each retire 21 buffers, 11 vertex arrays, and one texture with zero live delta. This supersedes the first fixed EMF/ETF visual/reload/accounting gap; additional aliases, entity types, ETF variants/emissive/blink passes, drivers, and remote-server behavior remain. | `emf-etf-zombie-render-reference.json`, the managed `emf-etf-zombie-render` fixture, checked baseline, `CemEntityExpressionContextFactory`, `SkeletalModelComposer`, `SkeletalLoader.reloadContentFidelity`, focused tests, and [EMF/ETF render evidence](../evidence/2026-07-26-emf-etf-render-reference.md). |
| Verified | The pinned Naturalist adapter mounts 32 entity geometry routes across 25 Gecko identities, owns all 33 artifact-derived remote entity definitions, and declares 25 generation-bound base-texture resolvers. Exact entity type/name, baby/aggressive state, and pinned tracked-data indices select source bases for birds, snakes, butterflies, dragonflies, lizards/tails, tortoises, snails, bears, lions, deer, and ducks before optional ETF composition. Zebra's pinned native renderer is an undilated Minecraft 1.20.4 horse layer; the adapter emits its adult default cuboids with shipped animation-bone aliases and excludes the unrelated two-legged ostrich geometry. The detached lizard tail remains a static route because its geometry references no current animation document. A managed 1.20.4 Fabric run completed a warning-free 79,462-byte Registry Sync v0 transaction and materialized all 33 definitions among 159 entity IDs. A second live run resolved two remote rattlesnakes through `geometry.snake` to `rattlesnake.png`; both rejection checkpoints balanced 187 buffers, 106 vertex arrays, and four textures with zero live delta, and accepted recovery preserved the route/texture. The entities were outside the camera frustum, so checked routed pixels, source overlays/held-food substitutions, and Naturalist animation parity remain. | `NaturalistCompatibilityAdapter`, `NaturalistNativeModels`, `GeckoLibEntityTextureRegistry`, `SkeletalLoader.entityTexture`, `EntityData.raw`, `FabricRemoteRegistrySync`, focused adapter/loader tests, live `both.ready`/`client.entities`/reload diagnostics, and [Naturalist registry-sync evidence](../evidence/2026-07-26-naturalist-registry-sync.md). |
| Verified | Gecko timeline effects now cross a generation-bound dependent-mod handler before platform playback. A content-identity resolver can explicitly map an alias, consume it, or pass through an intentional resource location; entity/item/armor and block-entity consumers bind the exact owner generation with their retained instance. Owner closure makes an old binding fail closed. The pinned Naturalist snake therefore maps only tongue `hiss` and suppresses its current `idle` plus handler-less `rattle` animation aliases instead of auto-playing fake Minecraft sound IDs. Focused and entity-integration tests cover mapping, pass-through, suppression, native positional audio, cleanup, and old-binding isolation; live controller/event and packet-sound timing remain gated. | `GeckoLibRuntimeEffectRegistry`, `GeckoLibEventPlayback`, `GeckoLibEntityEventConsumer`, `GeckoLibBlockEntityRenderer`, `NaturalistCompatibilityAdapter`, focused tests, and [Naturalist registry-sync evidence](../evidence/2026-07-26-naturalist-registry-sync.md). |
| Verified | Retained Gecko entity controllers can declare immutable bounded random-integer, exact-entity-type, and nearby-player range/AABB host-state inputs independently from protocol metadata. The renderer resolves only declared inputs; the headless controller surface accepts an explicit resolver, conflicting names reject construction, and query DTOs retain no owner callback across generations. Naturalist's independent tongue controller reproduces the pinned awake/stopped `nextInt(1000) < tickCount` play-once rule, while its rattle controller loops only for an awake rattlesnake with a living non-spectator player in the exact four-block and expanded `4×2×4` boundary. A real-GL local-authority scenario observes one retained 252-vertex body pass, all four controllers, the exact rattle loop, and advancing elapsed time across production content reload. This is controller/topology evidence, not a checked seam-pixel reference. | `GeckoLibHostStateInput`, `GeckoLibEntityHostStateResolver`, `GeckoLibControllerSet.inspect`, `SkeletalFeature`, `NaturalistCompatibilityAdapter`, `naturalist-controller-state.json`, focused API/adapter/entity tests, and [Naturalist registry-sync evidence](../evidence/2026-07-26-naturalist-registry-sync.md). |
| Verified | Iris shader settings now preserve adjacent CRLF boolean definitions, accept both authored profile-screen spellings, and are loaded by the exact external-pack gate. A bounded debug option transaction validates against that settings model, reloads through the shared resource event, and rolls the option store back on failure. On Apple OpenGL, disabling Complementary's WSR-only diagnostic path produced clean generation fingerprint `211f61d...663e` with empty rejection/fallback maps and the stable 40/7/106/0 texture/FBO/program/shader ledger. | `IrisShaderPackPlanner.options`, `IrisShaderPackPlanner.settings`, `IrisPresentationController.configureOptions`, `mods.iris.configure-options`, focused CRLF/settings tests, the exact Complementary gate, live `render.substrate`, and [Iris pipeline evidence](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Real chest, trapped-chest, ender-chest, and shulker-box block entities can be placed and restored through a bounded client canary without replacing occupied world state. All four increased the visible block-entity total, selected Complementary's exact skeletal-lightmap block route, emitted their expected Iris block-entity material IDs (5008/5012/5016), and retained empty rejection/fallback maps before exact air restoration. The shulker crossed final-framebuffer capture, but its steep-camera crop is not yet a checked model reference. | `StorageBlockEntityCanaryType`, `ClientDebugChannel.prepareStorageBlockEntity`, `World.set`, storage block-entity renderers, `render.substrate`, focused catalog tests, and [Iris pipeline evidence](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Iris particle billboards now publish a producer-derived non-cuboid lighting basis instead of world-up plus identity TBN. The generated geometry stage uses camera-right for increasing U, negative camera-up for increasing V, and the emitted triangle winding for the normal, preserving the UV domain's negative handedness. Focused mathematical/transformer tests and the exact Complementary gate pass. A live opaque/translucent sneeze canary rendered as a bounded crosshair billboard without the former screen-sized/dark rectangle, selected both particle contracts with empty rejection/fallback maps, and stopped advancing after exact cleanup. | `ParticleRenderer.particleBillboardAxes`, `IrisLegacyShaderTransformer.PARTICLE_GEOMETRY_BODY`, `ParticleBillboardAxesTest`, `IrisLegacyShaderTransformerTest`, `render.prepare-translucent-particle`, final-framebuffer capture, `render.substrate`, and [Iris pipeline evidence](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Billboard name/text geometry now derives its Iris lighting basis from the transformed local X/Y font plane, including the quad winding's negative handedness, instead of publishing fixed positive Z. A reversible client canary saves and restores exact tracked name/visibility plus render-only visibility override, allowing the ordinary name feature to execute even when another session points the camera away. Complementary selected the exact translucent billboard-text contract, cleanup stopped its counter, and rejection/fallback maps stayed empty. | `IrisLegacyShaderTransformer`, `BillboardTextMeshBuilder`, `ClientDebugChannel.prepareBillboardText`, focused transformer tests, the exact external-pack gate, `render.substrate`, and [Iris pipeline evidence](../evidence/2026-07-26-iris-render-pipeline-support.md). |

| In progress | The Apple OpenGL Complementary path now initializes `LOAD` targets deterministically, restores the cached framebuffer after depth blits, switches depth compare mode per reflected sampler type, and supplies bounded `frameTimeSmooth` plus uniform shadow-map size inputs. Focused tests pass, but the diverse-medium live trajectory still reproduced block-aligned/stippled corruption and eventually wedged the render thread with DH, entities, particles, and light shafts independently excluded. The identical pose was clean and responsive after disabling only Iris presentation, so this is an Iris target/material/fullscreen fidelity gate rather than a DH or world-embedding failure. Do not use the current Complementary Apple generation as visual-acceptance evidence; keep presentation disabled on the affected trajectory until a checked fix lands. | `IrisOpenGlRenderTargets`, `IrisShaderPackPlanner`, `IrisFrameState`, `programShadowComparisonSamplers`, focused tests, `/tmp/minosoft-base-renderer-original-2026-08-01.png`, and the [terrain consolidation implementation start](../evidence/2026-07-30-terrain-consolidation-start.md). |
| In progress | With Iris already disabled and the hierarchy fully hydrated, the diverse-medium fixed-pose DH A/B exposed a distinct foreground ownership defect: DH enabled drew giant dark page-aligned slabs across ready near terrain, while disabling only DH removed them immediately. The hierarchy still reported 510 active main pages, zero missing pages, zero allocation/upload failures, and only 117 masked pages while near coverage evolved. Zero missing/failure counters do not accept this visual boundary; diagnose coarse page-span masking against the frame-pinned near snapshot before changing page meshing. | `TerrainCoveragePageMask`, `DistantHierarchicalTerrainRuntime`, `NearSurfaceCoverageIndex`, `mods.distanthorizons.presentation`, and the [recent trajectory summary](../evidence/2026-08-01-recent-trajectory-summary.md). |

## Stable contracts

- Rendering consumes session/model state; it does not own authoritative world
  state or launcher flows.
- Respect graphics context/thread ownership for create, upload, draw, reload, and
  unload operations.
- Render phases, framebuffer, depth, blend, and culling state are shared
  contracts. Establish or restore state explicitly.
- Every active OpenGL sampler uniform must reference a texture unit with the
  matching target, even when application data never selects that array slot.
- Resource-pack dimensions are input data. Validate them against the driver
  limit and size array storage before issuing an upload.
- Simulation light values and GPU lightmaps are separate layers with an explicit
  invalidation/upload boundary.
- A missing baked model does not imply a visually inert block transition.
  Entity-backed placement, removal, and state changes must invalidate the
  section mesh that owns their alternate renderer.
- GUI index buffers must contain one six-index group per complete four-vertex
  quad. Never size or populate the index buffer from raw vertex count.
- Visual changes need more than a single screenshot: check relevant dimensions,
  time/weather, fog boundaries, and graphics profiles.

## Trajectory

**Target:** follow the
[render-substrate target architecture](../backlog/render-substrate.md). The flat
world pipeline and presentation-only Iris substitute have been removed. One
immutable graph/resource substrate now serves base, optimized-terrain,
shader-pack, and combined profiles. Built-in providers keep unmodded Minosoft
functional; selected compatibility providers replace ownership on the same
graph.

Program, shader-pipeline, render-target, and graph generations now use
last-known-good publication and frame leases. Static content and dynamic
texture arrays now prepare before publication, and each terrain-provider
generation pins an immutable vertex-layout declaration. A real independently
owned terrain backend must still prove that its physical layout buffers retire
with that generation. The current compatibility terrain provider is
graph-exclusive but continues to call Minosoft's scheduling, meshing, upload,
visibility, and batching core. The Iris adapter executes a bounded project
reference pack, not an independent real-world pack. Those are the next
architectural gates,
followed by visual, percentile-performance, and driver-side steady-state
baseline acceptance. Typed per-context OpenGL counts now provide the
measurement surface for that final gate.

Graphics extensions from mods must register through an owned scope so a mod
generation can build a candidate graph and enqueue render-thread cleanup before
its classloader is released. A hook's execution thread is the host call site's
thread: the frame boundary is the render thread, while entity visibility may
execute in asynchronous renderer preparation. Adapters must not assume those
are the same thread.
A whole-client generation reload must either dispose and recreate the graphics
context or keep a kernel-owned window/context shell behind a narrow stable API;
choose only after measuring context recreation cost and native ownership.

## Render-substrate checkpoint

| Status | Claim | Evidence |
| --- | --- | --- |
| Reported | SM0–SM6 are reported complete. Their useful outputs have been absorbed into the canonical implementation and dated checkpoint; synthetic duplicate graph/harness class names are not repository APIs. | [Render-substrate target](../backlog/render-substrate.md) and [R0–R7 checkpoint](../evidence/2026-07-24-render-substrate-r0-r7.md). |
| Verified | The production graph has stable IDs, explicit semantics/dependencies, deterministic validation, typed resource plans, transactional publication, and a CPU-testable execution surface. | `gui/rendering/graph/`, `RendererPipeline`, and focused graph/pipeline tests. |
| Verified | Base, Sodium, Iris-reference, and combined live profiles use the same graph; twenty invalid/valid Iris reload cycles preserve the selected generation and return lease counts to zero. | `render.substrate`, framebuffer captures, and [R0–R7 checkpoint](../evidence/2026-07-24-render-substrate-r0-r7.md). |
| Verified | Iris 1.7.2 `playerMood` and `constantMood` now originate in the world tick rather than a render-time placeholder. Registry biome mood settings drive one shared vanilla-compatible random light probe; the player accumulator resets and plays its configured sound at threshold, while the Iris constant accumulator clamps. An immutable frame snapshot uploads both linked uniforms, and live diagnostics proved nonzero advancing values across shader reload with stable GPU counts and no render fault. | `BiomeMoodSettings`, `BiomeMoodState`, `IrisFrameState`, `render.substrate.frameInputValues`, focused tests, and [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Iris 1.7.2 pack profiles now participate in generation rather than acting as display-only presets. Sequential inheritance, boolean/enumerated assignments, most-specific matching with stable authored-order ties, and `!program.*` suppression are applied before program pairing. Bounded slider and authored screen metadata maps into the native settings form, while exact Iris column-grid/localized presentation remains outside the claim. Live reload diagnostics report the selected profile; the reference pack retained all five terrain routes, an empty fallback map, and its 20/7/122/0 texture/framebuffer/program/shader baseline after reload. | `IrisShaderPackPlanner`, `ShaderPackSettings`, `IrisPresentationController`, `WorldShaderPipelineDiagnostics.selectedProfile`, focused tests, `render.substrate`, and [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Iris 1.7.2 stage-scoped local PNG textures, global `customTexture.<sampler>` PNG/raw aliases, adjacent metadata blur/clamp flags, custom PNG noise, and deterministic generated `noisetex` are immutable generation resources. Raw uploads cover 1D, 2D, 3D, and rectangle targets with bounded R/RG/RGB/RGBA byte/half/float formats, exact dimensions, typed sampler matching, Iris-compatible filtering defaults, and explicit tightly-packed OpenGL unpack state. Binary pack bytes participate in the fingerprint without entering GLSL option scanning. Buffer and custom samplers share one checked texture-unit allocation, custom bindings override ordinary buffer aliases, and candidate/reload closure owns every created OpenGL texture name. Namespaced resource descriptors are retained without inventing an upload; unknown active bindings fail on the missing source-native bridge. Complementary's exact managed block-atlas alias has the bounded array/layer bridge recorded below. Writable custom images remain unavailable on the Apple OpenGL backend because they require the Iris custom-image/OpenGL 4.2 capability. The untouched Complementary Unbound r5.8.1 planner gate passes, and the supervised client retained 51+6 scene routes, all five terrain/shadow routes, empty rejected/fallback maps, and a clean 42/7/103/0 texture/framebuffer/program/shader baseline. | `IrisShaderPackPlanner`, `IrisTexturePlan`, `IrisOpenGlCustomTextures`, `IrisOpenGlRenderTargets.bindSamplers`, focused planner tests, the external Complementary gate, `render.substrate`, and [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The official Iris example pack at commit `915c67d5f16584d1a1ec1f71b10c4f4af22939e1` crosses the optional external-pack planner gate and runs in the combined Iris/Sodium OpenGL client. Option-disabled unsupported stages are filtered after profile evaluation, terrain uses the Iris fallback chain through `gbuffers_textured`, and bounded GLSL 1.20 transforms adapt terrain, clouds, textured/basic sky, planets, generic texture draws, skeletal/lightmap/tinted entities, first-person arms, particles, and final presentation to exact host ABIs. The particle adapter expands retained points to quads in a generated geometry stage. Live diagnostics prove all three terrain material routes and all eight observed scene contracts selected pack programs with an empty fallback map; 31 textures, six FBOs, 47 programs, and zero shader objects remained live. A strict OpenGL probe found the generated sky bridge's former `vec3`/RGBA mismatch; the corrected `vec4` contract then ran without a subsequent `GL_INVALID_OPERATION`. The example has no shadow/composite roots, so those families remain gated on a second independent pack. | `IrisLegacyShaderTransformer`, `IrisProgramFallbacks`, `ShaderPipelineRegistry`, `FogManager`, `MINOSOFT_IRIS_TEST_PACK`, focused tests, `render.substrate`, and [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Photon at commit `15458c0937f8647c37eb6a501bef5eb3bf3da31b` plans and runs on Apple OpenGL with its compute-dependent skylight/reflection/cloud-shadow options disabled. The planner reads Iris buffer declarations retained inside block comments without exposing that metadata as executable GLSL; the live plan uses the pack's `R11F_G11F_B10F`, integer/HDR, resized, load, mipmap, and 2048² shadow descriptors rather than RGBA8 defaults. Fullscreen sampler units are reused per program within the driver's 16-fragment-sampler limit. Final presentation disables generic alpha blending and does not overwrite the pack's unit-zero `colortex0` binding with the host framebuffer texture. Explicit no-op cloud/basic-sky roots remain ABI-matched selected programs, so Photon suppresses host geometry instead of falling back to it. A 28-pass live frame executed 25 deferred/composite stages, pack terrain/shadow/final presentation, and every observed scene bind with an empty fallback map. Default Photon's active compute stage still rejects explicitly and is not claimed supported. | `IrisShaderPackPlanner`, `IrisLegacyShaderTransformer.suppressedScene`, `IrisOpenGlRenderTargets`, `FramebufferShader`, `IntegratedFramebuffer`, `IrisWorldShaderPipeline`, the optional external-pack test, `render.substrate`, and [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Official Complementary Unbound r5.8.1 now crosses the optional external-pack planner gate and reaches sustained real frames on Apple OpenGL with the historical managed `RP_MODE=1;SHADOW_QUALITY=1` Integrated PBR+ profile. Its GLSL 330 compatibility surface, sparse gbuffer outputs, scene variants, shadow terrain/entity programs, eight fullscreen stages, custom PNG/noise samplers, numbered custom-uniform smoothing, gameplay/biome expression inputs, and final presentation execute through the Iris-owned pipeline. Per-program host texture-array compaction and sparse pack-sampler allocation keep every linked fragment stage inside the driver's 16-sampler limit. The external-pack gate requires all 24 non-terrain scene-state ABIs plus the six entity-shadow caster states; the exact archive passes. Modern bridges cover player, billboard text, flashing block, generic 2-D overlays, world border, and calculated-PBR midpoint inputs, while an exact skybasic discard suppresses the duplicate host sun scatter. Persistent physical buffer-role alternation preserves double-buffered history across frames and removed the former dense volumetric-cloud stipple. Live player, named Naturalist snake, flame, and first-person fire-overlay canaries selected pack programs with an empty fallback map. Diagnostics reported 28 passes, 47 compiled main-view variants, six shadow variants, 15 logical buffers, 42 live textures, seven framebuffers, 99 programs, zero shader objects, eight advancing fullscreen programs, and one Iris presentation owner before and after transactional reload. This is executable Integrated PBR+ compatibility evidence, not authored companion-map evidence; the following true LabPBR checkpoint supersedes that material-output gap. | `modpacks/fabric-stack/shaderpacks/complementary-unbound.pw.toml`, `IrisTextureArrayLayout`, `IrisOpenGlRenderTargets`, `IrisShaderPackPlanner`, `IrisLegacyShaderTransformer`, `IrisCustomUniformEvaluator`, `IrisFrameState`, `LocalDisplayEntityFactory`, focused tests, the optional external-pack test, `render.substrate`, live framebuffer capture, and [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | OpenGL scene-state introspection now recognizes both ordinary uniforms and named uniform blocks. This restores the shared `uSkeletalBuffer` binding that Iris synchronization had skipped, uses byte-sized uniform-buffer ranges, and uploads exactly the packed transform span. Rebuilt real-GL captures show the full third-person player and a 252-vertex Naturalist Gecko model through Complementary main and shadow programs after hot reload, with 47+6 variants, 28 passes, one Iris presentation owner, and no fallback scene binds. Exact articulation seams, companion maps, fire-overlay pixels, and cross-driver references remain open. | `NativeShader`, `OpenGlNativeShader`, `OpenGlUniformBuffer`, `FloatOpenGlUniformBuffer`, `SkeletalManager`, focused buffer tests, `client.entities`, `render.substrate`, checked framebuffer captures, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Retained cuboid producers now deliver the real per-face UV midpoint consumed by Complementary Integrated PBR+. Generic/lightmapped skeletal, player, arm, shared block-feature, and first-person held-item meshes append a float2 face center; modern entity, block, and hand bridges derive the authored midpoint/sign/absolute face-local values instead of collapsing them to neutral zero. The later shared textured-quad checkpoint extends this domain to projected shadows and flame sheets; billboard text remains on its distinct transformed-font-plane ABI. Focused tests and the official external-pack gate pass; a rebuilt Apple OpenGL client linked 47+6 variants and rendered checked player/Naturalist pixels with no fallback binds. Companion maps, complete tangent-space parity, richer block-entity pixels, fire-overlay pixels, and another driver remain open. | `SkeletalMesh`, `PlayerModelMeshBuilder`, `BlockMeshBuilder`, `SimpleTextureMeshBuilder`, `IrisLegacyShaderTransformer`, focused midpoint/transformer tests, optional external-pack test, `render.substrate`, checked framebuffer captures, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | File-backed static textures now resolve aligned LabPBR `_n` and `_s` companions, require exact diffuse dimensions, and substitute the standard neutral normal/specular values when absent or invalid. Diffuse, normal, and specular occupy three vertical pages in the same OpenGL array layer, preserving the sampler and layer budget; transformed Iris material calls select the proper page while dynamic/font slots remain neutral and logical texture-size queries remain unchanged. Real-PNG tests preserve authored channels, the untouched Complementary external contract passes, and an Apple OpenGL generation linked 51 main plus six shadow routes. Skeletal block entities select `gbuffers_block/SKELETAL/SKELETAL_LIGHTMAP`; physical block-feature meshes also retain their block family inside translucent entity passes. At frame 7,948 every fullscreen stage advanced with no rejected or fallback scene binds. Animated companion timelines, checked authored-PBR pixels, page-boundary filter parity, complete tangent parity, and another driver remain open. | `LabPbrCompanionTextures`, `OpenGlTextureArray`, `IrisTextureArrayLayout`, `IrisProgramFallbacks`, `IrisLegacyShaderTransformer`, focused material/transformer/fallback tests, the optional external-pack test, `render.substrate`, live framebuffer capture, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The managed Complementary profile now selects `RP_MODE=3;SHADOW_QUALITY=1`, the pack's actual `CUSTOM_PBR`/LabPBR path. Entity, block, and hand bridges publish the POM-required tangent, binormal, view-vector, and atlas-midpoint varyings; retained cuboid midpoints remain real while producers without a tangent ABI use a stable orthonormal basis. A four-phase 16×64 fixture holds every diffuse pixel constant and independently switches neutral, normal-only, specular/emissive-only, and combined companion pages. Atomic `visual.sample` observations of the frame index and a fixed framebuffer region produced non-overlapping mean luminances of 0.09802, 0.09883, 0.10298, and 0.10353 respectively. This directly proves authored `_n` and `_s` output through the production real-GL pipeline rather than inferring it from upload counters. The run compiled all 24 non-terrain main-view state ABIs plus six shadow caster states, retained all five terrain routes, and reported empty rejection/fallback maps. The later particle alpha/presentation checkpoint supersedes the apparent dark/translucent double-render artifact; page-edge linear-filter parity and another driver remain open. | `IrisLegacyShaderTransformer`, `LabPbrCompanionTextures`, `OpenGlTextureArray`, `ClientDebugChannel.visualSample`, `IrisLabPbrRenderFixtureTest`, focused tests, the optional external-pack test, `render.substrate`, live framebuffer samples, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Iris frame capture now reads the lightning entity set under `EntityManager.lock`. This removes an intermittent `ConcurrentModificationException` when the network/session thread mutates entities while the render thread snapshots lightning inputs; a transient external play-parent restart remains distinguishable from a renderer failure through the supervised status protocol. | `IrisFrameStateClock.capture`, `EntityManager.lock`, `IrisFrameStateTest`, supervised play status, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Complementary Unbound's exact `customTexture.textureAtlas=minecraft:textures/atlas/blocks.png` contract now adapts to source-native texture arrays rather than a fabricated vanilla atlas. Its reflection face SSBO retains logical UV bounds while using the previously unused packed-color alpha byte and high half of `data.y` for the texture-array bucket and 16-bit layer. Reflected samples and size queries select that retained array/layer through the compacted `uTextures[]` layout. Other namespaced active resources remain fail-closed. The standard Iris environment now publishes `MC_OS_MAC` on macOS, so Complementary correctly disables its custom-image world-space-reflection path on Apple OpenGL. Focused metadata/sampling tests and the exact archive with `RP_MODE=3;SHADOW_QUALITY=1;WORLD_SPACE_REFLECTIONS=1` pass planning. A non-Apple runtime remains gated on custom images, compute/SSBO ownership, and cross-driver pixel evidence. | `IrisResourceTextureDescriptor`, `IrisShaderPackPlanner`, `IrisLegacyShaderTransformer.transformSourceNativeBlockAtlas`, `IrisOpenGlCustomTextures`, focused planner/transformer tests, the optional external Complementary gate, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The source-native block-atlas bridge now covers Complementary's high-feature fullscreen WSR consumer instead of assuming one draw-wide `atlasSize`. Option-preprocessed stage inspection limits face-data rewriting to active stages; reflected fragment samples, the legacy `tex` alias used by Integrated PBR material logic, and `atlasSize`/`gtextureSize` all select the array retained in that face record. Fullscreen and final programs that retain `uTextures` compile against the compact all-array layout, bind those host arrays, and reserve their texture units from Iris custom samplers. Raw unmarked mixed-draw texture-size uses remain fail-closed. The complete planner/transformer suites and the exact non-Mac Complementary configuration `COLORED_LIGHTING=128;WORLD_SPACE_REFLECTIONS=1` pass the complete host contract with `wsr_img`, `wsr_lod_img`, SSBO 0, and `shadowcomp.csh`. The opt-in production image/SSBO compute fixture opens a hidden Apple context and correctly stops at its OpenGL 4.1 capability gate; actual execution remains a non-Apple OpenGL 4.3+ lane. | `IrisLegacyShaderTransformer.transformSourceNativeBlockAtlas`, `IrisShaderPackPlanner`, `IrisTextureArrayLayout`, `IrisWorldShaderPipeline`, `OpenGlIrisCustomResourceComputeTest`, focused tests, the optional external Complementary gate, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The custom-resource ownership gap above is superseded at the implementation boundary. Iris `image.*`, `colorimg0..15`, `shadowcolorimg0..7`, and `bufferObject.0..8` references now produce bounded typed generation plans; OpenGL 4.2/4.3+ realization validates shared texture-image/SSBO limits, clears deterministically, resizes transactionally, binds the current double-buffer side with its actual internal format, emits barriers, and retires through normal resource accounting. Framebuffer-relative images and SSBOs resize from the prepared Iris main-target dimensions, including `worldScale`, before `setup*.csh` rather than following the OS window. Active `.csh` programs compile as compute-only programs: numbered fullscreen families execute before their paired graphics program, `setup*.csh` executes after first-frame/resize preparation and before `begin`, `shadow*.csh` executes after shadow depth clear and before shadow color clear/draw at shadow-map size, and `final*.csh` executes before final graphics at main-target size. Compute-only graph phases are retained. `indirect.<program>` retains an aligned, bounded pointer into a declared generation-owned SSBO and executes with `glDispatchComputeIndirect`, including runtime bounds after relative-buffer resize. The exact Complementary non-Mac high-feature gate retains `wsr_img`, `wsr_lod_img`, buffer 0, and `shadowcomp.csh`; the exact Apple gate truthfully retains none under `MC_OS_MAC`. A zero-image fast path avoids unsupported image-capability queries on Apple's OpenGL 4.1 context; the corrected hot-reloaded client reports zero current-generation OpenGL errors, empty custom resources/rejection/fallback maps, and the stable 40/7/103/0 texture/framebuffer/program/shader ledger. A real OpenGL 4.3+ WSR pixel/reload run and broader cross-driver coverage remain open. | `IrisCustomResourcePlan`, `IrisComputeDispatch.Indirect`, `ShaderProgramResourceUsage.renderTargetImages`, `IrisOpenGlCustomResources`, `IrisOpenGlRenderTargets`, `IrisComputeProgramSource`, `OpenGlNativeShader`, `IrisWorldShaderPipeline`, `RendererPipeline`, pinned `IrisRenderingPipeline`/`ComputeProgram` bytecode, `render.substrate.customShaderResources`, focused planner/render-graph tests, both exact Complementary gates, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Paired Iris `.tcs`/`.tes` stages now survive profile selection, include/option preprocessing, inspection, fingerprinting, active resource discovery, immutable planning, and native OpenGL linking behind a 4.0 capability/driver-limit gate. The compatibility transform ferries Minosoft's injected texture-array/layer, light, and fog varyings through control arrays and triangle-domain evaluation; selected staged programs set a three-vertex patch and matching complete triangle buffers draw as `GL_PATCHES`. Missing stage partners, non-triangle host-varying domains, incomplete/mismatched patches, and a tessellation-plus-geometry chain that needs injected host varyings fail before drawing or publication. Focused planner/transformer/draw tests and the untouched Complementary non-Mac regression gate pass. The opt-in production-path GPU fixture compiled all four stages on Apple OpenGL 4.1, drew the normal triangle buffer as patches, read the evaluation-only `(32, 128, 223, 255)` center pixel, and returned every tracked GL resource to zero. Complementary contains no staged programs; arbitrary patch topologies, an exact geometry-emission bridge, and a second driver remain open. | `ShaderProgramSource`, `ShaderManagement.createGraphics`, `OpenGlNativeShader`, `OpenGlVertexBuffer`, `OpenGlTessellationPixelIT`, `IrisLegacyShaderTransformer`, `IrisShaderPackPlanner`, `IrisWorldShaderPipeline`, `render.substrate.shaderPrograms.programStages`, focused tests, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | GLFW context creation asks for the capabilities the Iris substrate can use instead of always requesting the baseline: non-Apple startup requests 4.3 then 3.3; Apple requests 4.1 then 3.3; explicit legacy-quad mode retains its 3.0 compatibility context. Because the version hint is a minimum, supporting drivers may return a newer context without another request. The ordered fallback preserves normal multi-version startup while allowing tessellation, custom-image clears, compute, and SSBO paths to cross their existing capability gates on supporting drivers. Live bounded diagnostics on the restored Apple M4 Max process report `4.1 Metal - 90.5`, tessellation true, and image/custom-image/compute/SSBO false; the process retained 28 passes, 51+6 routes, empty rejection/fallback maps, and 40/7/103/0 shader-resource counts. | `OpenGlContextRequest`, `GlfwWindow`, `OpenGlCapabilityDiagnostics`, focused negotiation/capability tests, the opt-in tessellation GPU fixture, `render.substrate.gpuResources.capabilities`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| In progress | The non-Apple Iris backend gate is now wired into the Ubuntu CI matrix. It installs Mesa/Xvfb, forces the llvmpipe OpenGL 4.5 core profile, downloads and SHA-512-verifies the pinned Complementary Unbound archive, runs the exact non-Mac WSR/colored-lighting planner contract, and executes the production custom-image/compute/SSBO fixture in one X server. CI sets an explicit required-capabilities flag, so a missing image/compute/storage feature fails instead of producing a false-green JUnit skip; local Apple runs retain the expected optional skip. Workflow lint and the exact archive gate pass locally, but the new Linux job and checked WSR pixels still need an observed CI run. | `.github/workflows/build.yml`, `OpenGlIrisCustomResourceComputeTest`, `IrisShaderPackPlannerTest`, the pinned Complementary manifest, `actionlint`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Renderer-owned auxiliary targets now have an explicit host-shader scope. The entity-outline mask previously entered the `WORLD_OVERLAY` semantic, selected an Iris scene program, and let that program rebind the main world framebuffer; this produced a second opaque entity draw instead of a private mask. The internal-target scope keeps mask uniforms and geometry on the owned framebuffer, restores the prior scene binding, and leaves normal entity geometry Iris-routed. GUI/HUD remains explicitly presentation-side. A client-only glowing canary and checked Complementary capture showed one outlined villager, empty rejected/fallback maps, stable 40/7/103/0 shader resources, and exact flag restoration. | `ShaderPipelineRegistry.withInternalTarget`, `EntityOutlineRenderer`, `GUIShader`, `render.prepare-entity-outline`, focused routing/outline/entity-flag tests, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | World-border distance is measured against the translated, world-limit-clamped border faces rather than against world origin. Restoring a remote border now stops the retained canary mesh at both graph-layer and draw-time gates; its selected-contract counter remained exactly 175 across consecutive disabled snapshots. Pinned Iris 1.7.2 source establishes that the exact `TEXTURED` ShaderKey supplies a `GREATER 0.0001` alpha test unless `alphaTest.<program>` overrides it. The source-native pipeline now applies that unambiguous default to `GENERIC_TEXTURE_2D` and `WORLD_BORDER` variants, preventing fully transparent forcefield texels from writing Complementary auxiliary buffers. A radius-64 run reported distance 64 while selecting `WORLD_OVERLAY/TEXTURED/gbuffers_textured/WORLD_BORDER/WORLD_BORDER` with empty rejection/fallback maps and a stable 40/7/103/0 shader ledger. The exact external pack passed the full unit/integration suite, and a live transactional reload advanced graph/shader generations 17/2 to 18/3 without changing that ledger or fingerprint. Concurrent camera movement prevented a same-pose pixel comparison; an isolated checked border reference remains open. | `WorldBorder.getDistanceTo`, `WorldBorderRenderer`, `IrisAlphaTestDefaults`, `ClientDebugChannel.prepareWorldBorder`, `IrisLegacyShaderTransformer`, focused border/transformer tests, pinned Iris `ShaderKey`, the optional external-pack test, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | A reversible client-only lightning canary enters through `WorldEntities`, the normal entity renderer factory, retained crossed-ribbon geometry, and the translucent entity graph layer. Its isolated mesh now appends the real face normal for each crossed ribbon: both normals are derived from the exact jagged segment direction and their respective width axes, then transformed by the modern Iris lightning bridge instead of substituting world-up and identity TBN. Focused math proves normalized perpendicular directions; the transformer, exact Complementary WSR gate, and renderer integration pass. Complementary selected `ENTITIES_TRANSLUCENT/LIGHTNING/gbuffers_lightning/POSITION_COLOR/LIGHTNING`; exact removal stopped the new-generation counter at 654 across frames 781→1,029 with empty rejection/fallback maps and 40/7/106/0 resources. Concurrent camera/debug mutation prevented a checked pixel reference. | `LightningMeshBuilder`, `LightningRibbonBasis`, `LightningBoltFeature`, `IrisLegacyShaderTransformer`, `ClientDebugChannel.prepareLightning`, focused tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Primed TNT now has direct source-native Iris producer evidence rather than only bridge compilation. A reversible client-only entity canary enters through `WorldEntities`, the normal primed-TNT renderer, retained TNT block mesh, animated flash uniforms, entity graph layer, and physical shadow traversal. Complementary selected `ENTITIES_TRANSLUCENT/BLOCK/gbuffers_block/BLOCK_FEATURE/FLASHING_BLOCK` and its `shadow/BLOCK_FEATURE/FLASHING_BLOCK` counterpart; after removal their queued draws retired at 1,650/2,195 binds and remained stable through frame 1,046 with empty rejection/fallback maps and the stable 40/7/103/0 shader ledger. The first run also exposed an inverted gravity constant that launched TNT upward; corrected physics settles the retained producer from Y 63.25 to Y 63 before removal, with focused downward-motion coverage. | `ClientDebugChannel.preparePrimedTnt`, `PrimedTNTEntityRenderer`, `FlashingBlockFeature`, `PrimedTNTPhysics`, focused renderer/physics coverage, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | A reversible client-only beacon canary now enters through the registry block state, production `World.set` mutation boundary, normal `BeaconBlockEntity` creation, section remeshing, `ChunkRenderer` block-entity collection, and the retained translucent beam renderer. Complementary selected `BLOCK_ENTITIES_TRANSLUCENT/BEACON_BEAM/gbuffers_beaconbeam/POSITION_TEXTURE/BEACON_BEAM`; ordinary skeletal block entities simultaneously selected `BLOCK_ENTITIES/ENTITY/gbuffers_block/SKELETAL/SKELETAL_LIGHTMAP`. After restoring the exact air state and completing the asynchronous remesh, the beacon counter remained exactly 8,045 through frame 1,531 with empty rejection/fallback maps and the stable 40/7/103/0 shader ledger. | `ClientDebugChannel.prepareBeacon`, `World.set`, `BlockEntityDataProvider`, `ChunkRendererChangeListener`, `BeaconBeamRenderer`, focused block-entity/remesh/renderer tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Sign block-entity NBT changes now invalidate the owning terrain section, and repeated invalidations for a queued section replace its pending build identity with the latest terrain revision. This closes the same-frame place-then-text race that previously cancelled the stale build without requeueing. A reversible glowing-sign canary produced one loaded text mesh, submitted `EMISSIVE_ADDITIVE`, and selected Complementary's `minosoft:main/emissive_additive/gbuffers_terrain` route with empty rejection/fallback maps. Restoring air removed the text mesh and stopped its bind counter at 99 through frame 2,928; the shader ledger remained 40/7/103/0. | `BlockEntityDataUpdate`, `Chunk.applyBlockEntityData`, `BlockDataS2CP`, `ChunkRendererChangeListener`, `ChunkMeshingQueue`, `ClientDebugChannel.prepareSignText`, `render.substrate.terrain.visibleMeshes`, focused chunk/remesh tests, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | A reversible client-only block-break canary now publishes one bounded `BlockBreakAnimationEvent` for an existing nearby model-bearing block. The ordinary retained overlay renderer, destroy-stage texture, polygon-offset world-overlay pass, and damaged-block scene contract remain authoritative; no block or server state is mutated. Complementary selected `WORLD_OVERLAY/DAMAGED_BLOCK/gbuffers_damagedblock/DAMAGED_BLOCK/DAMAGED_BLOCK`; after event removal the counter remained exactly 450 through frame 253 with zero retained instances, empty rejection/fallback maps, and the stable 40/7/103/0 shader ledger. | `ClientDebugChannel.prepareBlockBreak`, `BlockBreakAnimationEvent`, `BlockBreakRenderer`, `BlockBreakShader`, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | A reversible client-only weather canary now applies full rain while preserving the production weather overlay, authored rain texture, mesh, intensity/offset uploads, semantic graph phase, and exact scene contract. When the current biome has no precipitation, it overrides only that render gate with `RAIN`; restore is conditional so concurrent authoritative weather changes are not overwritten. Complementary selected `WEATHER/WEATHER/gbuffers_weather/WEATHER/WEATHER`; restoration returned rain to zero and the gate override to null, after which the route counter remained exactly 600 through frame 248 with empty rejection/fallback maps and the stable 40/7/103/0 shader ledger. | `ClientDebugChannel.prepareWeather`, `WeatherOverlay.referencePrecipitationOverride`, `OverlayManager.get`, `WeatherOverlayShader`, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Authored ETF/EMF and Gecko emissive companions already enter the retained skeletal translucent layer and select `ENTITY_EYES`. A reversible acceptance-only override now lets a scene without such an authored companion route one retained skeletal feature's resolved base material through that same production layer. Complementary selected `ENTITIES_TRANSLUCENT/ENTITY_EYES/gbuffers_spidereyes/SKELETAL/SKELETAL_TINTED`; restore removed the material and visibility overrides, its queued draws retired at 575 binds and remained stable through frame 810, both rejection/fallback maps stayed empty, and the shader ledger remained 40/7/103/0. | `SkeletalFeature.drawEmissive`, `SkeletalFeature.referenceEmissiveBaseOverride`, `ClientDebugChannel.prepareEntityEyes`, emissive material loader tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Modern `gbuffers_basic` now receives the same retained color, light-color, and sky-color specializations already applied to modern line programs. Before the fix, a real client-only leash selected its normal ribbon producer but accumulated `ENTITIES/LEASH/POSITION_COLOR_LIGHT/LIGHT_COLOR` host fallbacks. After hot reload, the active Complementary generation retained `gbuffers_basic/POSITION_COLOR_LIGHT/LIGHT_COLOR` and selected `ENTITIES/LEASH/gbuffers_basic/POSITION_COLOR_LIGHT/LIGHT_COLOR`; exact holder/visibility restoration left empty rejection/fallback maps and the route counter remained exactly 258 through frame 654. The three newly linked basic variants intentionally move the stable shader ledger from 40/7/103/0 to 40/7/106/0. | `IrisLegacyShaderTransformer`, `LightColorShader`, `EntityLeashFeature`, `ClientDebugChannel.prepareLeash`, focused transformer/projector tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The deferred-sensitive translucent-particle graph path now has direct production evidence. A reversible client-only sneeze particle enters through the ordinary queue/ticker, authored alpha, translucent particle mesh builder, post-deferred graph phase, `PARTICLE_POINT/PARTICLE` ABI, and Iris scene binder. Complementary selected `PARTICLES_TRANSLUCENT/PARTICLE/gbuffers_particles/PARTICLE_POINT/PARTICLE`; after the exact canary was marked dead, queued draws retired at 924 binds and remained stable through frame 454. Rejection/fallback maps stayed empty and the shader ledger remained 40/7/106/0. | `ParticleRenderer`, `ParticleQueue`, `ParticleTicker`, `TextureParticle`, `SneezeParticle`, `ClientDebugChannel.prepareTranslucentParticle`, focused lifecycle tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The generic basic-color producer now has direct live evidence rather than only modern bridge compilation. A reversible chunk-border gate leaves the user's F3+G profile untouched while the normal async `LineMeshBuilder`, world-overlay layer, and `ColorShader` render the complete chunk/section grid. Complementary selected `WORLD_OVERLAY/BASIC/gbuffers_basic/POSITION_COLOR/COLOR`; restore returned the override to null and effective state to the configured false value, after which queued draws retired at 115 binds and remained stable through frame 572. Rejection/fallback maps stayed empty and the shader ledger remained 40/7/106/0. | `ChunkBorderRenderer.referenceEnabledOverride`, `ClientDebugChannel.prepareChunkBorder`, `IrisLegacyShaderTransformer`, focused gate coverage, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Shared world `POSITION_TEXTURE` quads now append a producer-derived normal, UV tangent/handedness, and four-corner face midpoint instead of relying on world-up, identity TBN, or a zero-radius material domain. Projected entity shadows, local camera-facing flame sheets, and all four beacon prism sides enter through one quad-basis boundary; modern generic/flame/beacon Iris bridges consume and transform the basis, while generic/flame pass the retained midpoint into Complementary's calculated-PBR coordinate helper. Direct-vertex and screen-space users retain bounded neutral defaults. Focused horizontal/vertical/mirrored-UV/midpoint tests, transformer coverage, the exact Complementary WSR external gate, the complete root unit suite, and focused shadow/flame/beacon integrations pass. On clean generation 18/3, live canaries selected generic-shadow, flame main/shadow, billboard, and beacon routes with empty rejection/fallback maps and 40/7/106/0 resources; exact cleanup left flame at 1,080, beacon at 1,155, and billboard at 2,050 through frames 1,955→1,976. A concurrent controller invalidated the visual pose, so an isolated same-pose pixel reference remains open. | `SimpleTextureMeshBuilder`, `SimpleTextureQuadBasis`, `EntityShadowFeature`, `EntityFlameFeature`, `BeaconBeamRenderer`, `IrisLegacyShaderTransformer`, focused tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Leash ribbons now append their producer-derived front-winding normal instead of lighting every crossed face as world-up. Complementary's non-line `gbuffers_basic` path consumes that normal in `DoLighting`; its `GBUFFERS_LINE` specialization explicitly skips the branch, so ordinary color lines retain their smaller ABI. Collapsed vertical/zero-length ribbons receive distinct finite fallbacks for their two crossed orientations. Focused geometry/transformer tests, the exact Complementary WSR gate, and real leash integration pass. A reversible live cat leash selected `ENTITIES/LEASH/gbuffers_basic/POSITION_COLOR_LIGHT/LIGHT_COLOR`; exact holder/visibility cleanup stopped its counter at 1,236 through frames 2,292→2,320 with empty rejection/fallback maps and 40/7/106/0 resources. | `EntityLeashProjector.RibbonQuad`, `LightColorMeshBuilder`, `EntityLeashFeature`, `IrisLegacyShaderTransformer`, `ClientDebugChannel.prepareLeash`, focused tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | World-border faces now append inward, front-winding cardinal normals instead of inheriting the camera-plane normal used by screen overlays. Complementary's `gbuffers_textured` consumes this input in `DoLighting`; only `WORLD_BORDER` reads location 3, leaving `POSITION_TEXTURE_2D` overlays unchanged. Focused face/transformer tests and the exact Complementary WSR gate pass. A radius-64 live canary selected `WORLD_OVERLAY/TEXTURED/gbuffers_textured/WORLD_BORDER/WORLD_BORDER`; exact center/radius/gate restoration stopped it at 615 binds through frames 1,146→1,150 with empty rejection/fallback maps and 40/7/106/0 resources. | `WorldBorderMeshBuilder`, `IrisLegacyShaderTransformer`, `ClientDebugChannel.prepareWorldBorder`, focused tests, `render.substrate`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | Iris program and per-render-target blend directives are typed, capability-gated, and applied without leaking indexed state into host/internal draws. OpenGL 4.0 core and `ARB_draw_buffers_blend` extension contexts share one feature predicate from preprocessing through runtime dispatch. The planner uses the final active `DRAWBUFFERS`/`RENDERTARGETS` declaration, fixing Complementary WSR water's former `03` truncation. On Apple OpenGL 4.1, `gbuffers_water` wrote `0,3,6,4,8`; its `colortex4`/`8` blend disables were each applied 646 times, then restarted after transactional shader reload while the 42/7/106/0 texture/FBO/program/shader ledger and empty rejection/fallback maps remained stable. | `IrisProgramBlendOverride`, `IrisShaderPackPlanner`, `IrisOpenGlRenderTargets`, `OpenGlCapabilityDiagnostics`, `ShaderPipelineRegistry`, focused tests, the renderer-pipeline integration test, exact Complementary external-pack/live gates, `render.substrate.shaderPrograms.appliedBlendOverrides`, and the [Iris pipeline boundary](../evidence/2026-07-26-iris-render-pipeline-support.md). |
| Verified | The active untouched Complementary generation now has physical producer evidence for every compiled main-scene vertex and state ABI rather than planner-only coverage. A reversible one-at-a-time sweep exercised entity specials, block features/entities, weather/sky, particles, hand, and world overlays; `unsubmittedCompiledSceneVertexAbis` and `unsubmittedCompiledSceneStateAbis` were both empty afterward. Main/shadow opaque, cutout, and translucent terrain, both depth-copy boundaries, and every selected fullscreen stage advanced; selected physical shadow casters respected the pack's authored exclusions. Rejected/fallback maps remained empty, cleanup restored clear weather and the exact player pose, and the ledger stabilized at 40 textures, one renderbuffer, seven FBOs, 111 programs, and zero shader objects. Integration-source compilation and the focused renderer/chunk integration matrix pass. GUI/HUD remains correctly presentation-side after final composition. | Reversible `ClientDebugChannel` render preparations, `render.substrate`, `state.sample`, `RendererPipelineTest`, `ChunkRendererTest`, and the [producer-complete Iris checkpoint](../evidence/2026-07-26-iris-render-pipeline-support.md#producer-complete-complementary-execution-checkpoint). |
| Verified | The block-entity shadow route now has positive pack-authored execution evidence rather than only Complementary's intentional exclusion. A focused pack enables only general block-entity shadows and exposes exactly one skeletal-lightmap shadow bridge. A real chest entered through normal world mutation, cache, and renderer paths and physically submitted `BLOCK_ENTITIES/ENTITY/shadow/SKELETAL/SKELETAL_LIGHTMAP`; terrain/entity caster classes were disabled, so they cannot explain the traffic. The actual Gecko block-entity renderer test proves opaque layers stay base-only while translucent/additive layers opt into the same single pre-snapshot submission. Transactional discovered-pack selection restored the official archive and exact options, empty rejection/fallback maps, stable resources, and exact player pose. A checked translucent-layer shadow pixel remains open. | `iris-block-entity-shadow`, `IrisPresentationController.selectDiscoveredPack`, `GeckoLibBlockEntityRendererTest`, `BlockEntityRendererTest`, `render.prepare-storage-block-entity`, `render.substrate`, and the [positive block-entity shadow checkpoint](../evidence/2026-07-26-iris-render-pipeline-support.md#positive-independent-block-entity-shadow-checkpoint). |
| Verified | Dynamic and static content texture replacements preserve last-known-good handles on candidate failure. Terrain backend generations snapshot the negotiated vertex layout and retain that declaration through frame/backend retirement. | `OpenGlDynamicTextureArray`, `StaticTextureArrayUpdate`, `TerrainBackendRegistry`, and focused headless tests. |
| Verified | The current world-producer surface is Iris-route complete. Source inspection finds no world renderer registered with an implicit `AUTO` semantic; scene shaders fail closed outside a semantic or explicit internal-target scope. On the final Complementary generation, all 22 reversible production canaries prepared and restored, all 19 compiled scene vertex ABIs and 24 scene state ABIs physically submitted, opaque/cutout/translucent/emissive terrain selected pack programs, all three shadow terrain materials and all eight fullscreen stages advanced, and rejection/fallback maps remained empty. Earlier focused packs independently prove general block-entity, player-only, and light-block-entity-only caster ownership. GUI/HUD remains after Iris final presentation, matching Iris's upstream world-pipeline boundary rather than being injected as duplicate world geometry. Remaining checked pixels, drivers, and performance work are fidelity/portability gates, not host-shader escape paths. | Explicit `WorldPassRegistry.add` declarations, `ShaderPipelineRegistry.requireFrameSceneSemantic`, all `render.prepare-*` canaries, `render.substrate`, focused shadow packs, and the [current completion audit](../evidence/2026-07-26-iris-render-pipeline-support.md#current-world-producer-completion-audit). |
| Verified | The apparent multi-state sand rendering in the ordinary Fabric stack was the intentionally animated `iris-labpbr-render` acceptance fixture, not terrain snapshotting, scheduling, vertex publication, or visibility. The fixture now remains integration-test-only and is absent from ordinary `fabric-stack` mounting. After transactional pack reload, six one-second sand-region samples reported no material-animation state and only smooth daylight luminance movement instead of the fixture's discrete repeating phases. | `modpacks/fabric-stack/fixtures.tsv`, `IrisLabPbrRenderFixtureTest`, live `visual.sample`, and [screenshot/terrain evidence](../evidence/2026-07-28-screenshot-and-terrain-stability.md). |
| Verified | Complementary Unbound's moving cloud and distant-foliage grain is authored stochastic sampling that depends on temporal convergence, not duplicate terrain submission or a missing history swap. A live TAA-off A/B made the grain substantially worse; the managed profile now combines TAA smoothing level 4, motion-retained FXAA, high cloud sampling, and 8x anisotropic terrain filtering. Enabling anisotropy exposed and closed a source-native atlas gap: the modern terrain transformer rewrites Complementary's sampler-parameter `textureAF` helper and explicit LOD taps onto the selected texture array/layer. The separately translucent first-person hand came from omitted pinned Iris cutout defaults: `ARM` and `HELD_ITEM` scene routes now receive `GREATER 0.1` unless the pack authors an override, preventing zero-alpha sleeve texels from updating depth. A real-OpenGL controlled camera turn settled at roughly 70–78 FPS with advancing hand/cloud/fullscreen routes, empty rejection/fallback maps, stable 40/7/111/0 Iris resources, and the exact test pose restored. Fine pack-authored motion grain remains a quality/performance tradeoff. | `IrisAlphaTestDefaults`, `IrisLegacyShaderTransformer.transformModernTerrainFragment`, `IrisLegacyShaderTransformerTest`, Complementary's managed shader options, pinned Iris 1.7.2 `ShaderKey` bytecode, `render.substrate`, live screenshots, and [Complementary motion/hand evidence](../evidence/2026-07-28-complementary-motion-and-hand.md). |
| Verified | Camera-motion noise now has a repeatable quantitative gate instead of screenshot-only judgment. The probe returns a five-degree yaw turn to the exact pose and compares each recovery capture against an equal-render-frame stationary control. A two-sample 3456×850 cloud-region run at representative cadence measured immediate flat-region changes at 5.13% versus 0.12% control (43.56×) and luma error at 5.77× control; by the 32-frame checkpoint those ratios converged to 1.15× and 1.07×. This confirms a strong motion-triggered transient that mostly converges, while keeping the remaining cause/fidelity decision separate from the metric. | `MotionNoiseAnalyzer`, `Play.debugMotionNoise`, live report, and [camera-motion noise evidence](../evidence/2026-07-28-camera-motion-noise-measurement.md). |
| Verified | The managed Complementary path pins zero TAA jitter and disabled foliage/leaves waving. A revision-anchored temporal transformer supplies previous-wind terrain positions through `RGBA16F colortex14` when waving is explicitly re-enabled, defensively strips authored projection jitter from entity/hand material-254 routes that bypass TAA, bypasses every history sample and clamp underwater while publishing the current fogged color as the next history value, tapers and mip-stabilizes distant foliage, and adds world-reprojected/disocclusion-rejected cloud depth through double-buffered `R16F colortex15`. Both final and waving-enabled exact-archive planner gates pass; both candidates compile on Apple OpenGL, the enabled live candidate writes `colortex14`, the restored final generation writes cloud history with empty rejection/fallback maps, and an underwater TAA-on live capture remains spatially smooth after the early-return fix. | `IrisTemporalStabilityTransformer`, focused tests, exact Complementary planner gates, `render.substrate`, and [temporal-stability evidence](../evidence/2026-07-29-complementary-temporal-stability.md). |
| Verified | Full-screen simple overlays transform logical UVs into the static texture's physical diffuse page before mesh submission. Iris plans also type `underwaterOverlay` with the upstream-compatible true default, and `WaterOverlay` consults the immutable frame-pinned plan before submitting the vanilla effect. This prevents a pack such as Complementary, which declares `underwaterOverlay=false` and owns underwater fog/post-processing, from receiving both effects through its MRT pipeline. A live underwater hot reload removed the wrong-page bands, black contours, and speckling while restoring the untouched Complementary fingerprint and the stable 44-texture/7-FBO/111-program/0-shader ledger. | `SimpleOverlay`, `WaterOverlay`, `ShaderPipelinePlan`, `IrisShaderPackPlanner`, focused tests, pinned Iris `MixinScreenEffectRenderer` bytecode, `render.substrate`, and [temporal-stability evidence](../evidence/2026-07-29-complementary-temporal-stability.md#underwater-overlay-compatibility). |
| Verified | Complementary r5.8.1 water now remains legible in block-lit caves and preserves local-light contrast underwater. A revision-anchored resolved-source transformer drives water-normal amplitude from the stronger block- or sky-light channel and makes the pack's surface attenuation luminance-aware while retaining its volumetric blue attenuation. Managed alpha, fog, bumpiness, and underwater color options provide a distinct but translucent surface and bounded underwater depth cue. The exact archive planner gate, focused tests, and `assemble` pass. A clean Apple OpenGL generation advanced water, all shadow terrain classes, and all fullscreen stages with empty rejection/fallback maps and a stable 44-texture/7-FBO/111-program/0-shader ledger; client/server block samples agreed and the exact pre-test pose and render suppressions were restored. | `IrisComplementaryWaterTransformer`, Complementary's managed shader options, focused tests, `render.substrate`, final live captures, and [water-lighting correction evidence](../evidence/2026-07-29-complementary-temporal-stability.md#water-lighting-correction). |
| Verified | The optimized near provider and provider-neutral distant hierarchy now share process scheduling, semantic meshing, transactional region upload/storage, frame-pinned visibility/publication, batching, telemetry, and lifecycle ownership. The sole production route passed built-in/Bliss checked seams, retained performance records, reload/resource cleanup, and the post-retirement 15-minute streaming soak. Specialized non-terrain pixel canaries, broader PBR references, and a second-driver resource/reload baseline remain portability/fidelity work rather than missing terrain ownership. | `TerrainProcessBuildService`, `OpenGlNearTerrainRegionRuntime`, `DistantHierarchicalTerrainRuntime`, [terrain consolidation evidence](../evidence/2026-07-30-terrain-consolidation-start.md), and the retained performance/soak JSON records. |
| Verified | The post-consolidation render path removes the remaining obvious per-frame/per-update churn: owned near stream arrays flow through shared immutable upload operations directly into headless/OpenGL destinations; near page ordering/grouping and main/shadow section membership are planned once per view/frame; entity shadow/leash meshes retain GPU names and update fixed-capacity vertices in place (shadow buffers grow geometrically only when required); entity shadow bounds are culled once during collection; and the near/distant descriptor boundary shares one `TerrainMaterialClass` contract. | `TerrainArtifactBuffer`, `TerrainUploadOperation`, `OpenGlTerrainRegionDevice`, `OpenGlNearTerrainRegionRuntime`, `ChunkRenderer`, `EntityShadowFeature`, `EntityLeashFeature`, `EntityDrawer`, and focused compile/unit checks. |

## Next evidence

1. Run the consolidated terrain resource/reload gate on a second OpenGL driver;
   the available Apple OpenGL 4.1 host has completed the local implementation,
   checked-pixel, performance, lifecycle, and soak matrix.
2. Extend the bounded legacy transformer only as new retained producer ABIs
   require it; keep unsupported pack roots, non-triangle host-varying
   tessellation domains, and tessellation/geometry host-varying chains
   fail-closed while compute and paired triangle tessellation remain
   capability-gated.
3. Expand the deterministic scene to visibly exercise every graph producer:
   terrain material classes, entities, block entities, particles, weather,
   overlays, hand, HUD, shadow view, and composite.
4. Use the typed driver-side program/texture/buffer/framebuffer counts to accept
   stable reload/unload baselines, including dynamic/static texture swaps and
   physical layout buffers from the first independently owned terrain backend.
5. Recreate the R0 base percentile baseline and meet the target p95 frame,
   preparation, and submission thresholds for optimized-terrain and combined
   profiles.
6. Rerun the four-profile visual matrix, invalid/valid reload loop, unload, and
   multi-version/headless gates. Accept R0–R7 only when every completion
   predicate is true.
7. Continue content-fidelity material and renderer work through the canonical
   graph rather than adding new direct shader/pass ownership seams.

## Validation

- Matching unit/integration packages for CPU-side logic
- Model, texture, tint, and packet/chunk fixtures under integration resources
- `./gradlew compileKotlin` plus a visual smoke test for GPU lifecycle/state work
- Reload acceptance: valid change becomes visible; invalid change preserves the
  prior resource; repeated reload does not grow GPU/resource counts

## References

- [Rendering overview](../../rendering/ReadMe.md)
- [Complementary Unbound integration](../../rendering/ComplementaryUnbound.md)
- [Content and asset system](../../Assets.md)
- [Meshes](../../rendering/Meshes.md)
- [Entities](../../rendering/Entities.md)
- [In-game GUI](../../rendering/GUI.md)
- [Shaders](../../Shader.md)
